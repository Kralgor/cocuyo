# Handoff — 2026-05-16

## What Was Completed This Session

**No code written.** Session was a design grilling to resolve all UI implementation decisions before coding begins. 15 questions resolved covering scope, themes, layout, data wiring, report flow, and performance.

## Decisions Made (All Locked)

| # | Decision | Detail |
|---|----------|--------|
| 1 | Scope | Full 5-tab shell. Tabs 3-5 (Pronóstico, Bajones, Historial) show teaser with static demo data at 50% opacity + "Próximamente" overlay |
| 2 | Themes | Two only: `tinta` (dark, default) + `estudio` (light). Toggle in settings. |
| 3 | Mi zona tab | Shows region-level data in Phase 1. Label stays "Mi zona". Zone drill-down deferred Phase 3. |
| 4 | Region selection | First-launch picker (17 cities grouped by state) → localStorage `cocuyo_region`. Title bar shows region + tap to change. |
| 5 | Styling | Hybrid: inline styles + CSS vars on `:root` + globals.css for keyframes/fonts/pseudo-classes. No new deps. |
| 6 | Report flow | One tap → immediate POST → inline "Tú + X otros · 30 min" → 60s undo link → 60s client-side cooldown. No confirmation modal. |
| 7 | Map interaction | Tap marker → switch to Mi zona tab showing that region + back arrow to return to saved region. |
| 8 | Desktop | Mobile-only layout, max-width centered on desktop. Desktop dashboard deferred Phase 2. |
| 9 | Signal fingerprint | Full 4-wedge radial rendered. 3 wedges ghosted at 0.15 opacity with lock icon (Phase 1 only has crowd). |
| 10 | Power-back shortcut | Sticky banner top of Mi zona when localStorage has `no_power` < 12h. One tap → submit → dismiss. |
| 11 | Fonts | `next/font` self-hosted: Inter (body), Instrument Serif (display), JetBrains Mono (mono). Latin subset. |
| 12 | Leaflet loading | Prefetch via `requestIdleCallback` after initial paint. Map tab ready by time user navigates. |
| 13 | i18n | Bilingual es/en. `tt(key, lang)` string table. Language toggle in settings. Spanish default. |
| 14 | Teaser screens | Static demo data from `cocuyo-data.js` rendered at 50% opacity + "Próximamente · Phase N" overlay. |
| 15 | Rationing patterns | Accent-bordered callout in Mi zona, conditional render when `rationing_pattern` non-null in status.json. |

## What's Next

**New task needed: T-029-UI (or similar) — Full frontend redesign.**

All pipeline tasks (T-001 through T-028) are complete. Phase 5 (T-029–T-035) is forbidden. The next work is implementing the UI from `docs/claudedesign/` using the 15 decisions above.

### Implementation Order

```
1. lib/theme.ts + lib/i18n.ts                    (foundation)
2. styles/globals.css (fonts + keyframes + vars)  (foundation)
3. contexts/AppContext.tsx                        (foundation)
4. components/primitives/* (8-10 components)      (parallelizable)
5. components/mobile/MobileShell + TabBar         (shell)
6. pages/index.tsx rewrite (tab switching)        (shell)
7. First-launch region picker                     (flow)
8. components/mobile/ScreenZoneDetail             (tab 1 — primary)
9. components/mobile/ReportButtons + undo         (tab 1 — action)
10. components/Map.tsx upgrade (firefly markers)  (tab 2)
11. Teaser screens (Forecast, Bajones, History)   (tabs 3-5)
12. Power-back banner                             (UX polish)
13. Theme toggle + language toggle                (settings)
```

## Blockers

None. All backend infrastructure complete. `.env` exists with credentials. `status.json` generates correctly. Frontend can be developed in isolation using local `status.json`.

## Key Source Files

- Design prototypes: `docs/claudedesign/cocuyo-*.jsx` + `cocuyo-data.js`
- Current frontend: `app/pages/index.tsx`, `app/components/*.tsx`, `app/lib/api.ts`
- Theme/data reference: `docs/claudedesign/cocuyo-data.js` (themes, i18n strings, mock scenarios)
- Pipeline regions (source of truth): `pipeline/regions.py`

## Suggested Skills for Next Session

- Start with implementation — no further planning needed
- Use `/tdd` for primitives that have clear input/output (Fingerprint SVG, ForecastCurve)
- Use `/simplify` after each batch of components to catch duplication
- Use `/diagnose` if bundle size exceeds 250KB target

## Context Not Captured Elsewhere

- The 17 regions in `pipeline/regions.py` differ slightly from the 14 in `cocuyo-data.js` (design prototype). Use `regions.py` as canonical — it has `barcelona`, `guarenas_guatire`, `los_teques` which the design omitted.
- Design's `FOCUS_ZONE` (El Paraíso) is aspirational — no zones exist in Phase 1. Ignore zone-specific UI elements (feeder circuit, homes count) until Phase 3.
- `cocuyo-viz.jsx` contains all custom SVG visualization code. Port 1:1 to typed TSX — behavior unchanged.
