# Continue Here — Cocuyo Mobile

**Paused:** 2026-08-14 (Phase 5 code-complete; store submission human-gated)
**Milestone:** v1.0 — 5/5 phases code-complete
**HEAD:** `522125c` feat(05): history tab + store submission config

---

## Resume in one line

All buildable work is done. The only remaining items are HUMAN-GATED (see below). If resuming a fresh session: re-run `npm test --prefix mobile`, `npx tsc --noEmit`, `npx expo lint` (all in `mobile/`), and `python3 -m pytest tests/ -q` (in `pipeline/`) to confirm green.

---

## Exact position

**Phase 5 (Polish + Store Submission) — CODE-COMPLETE 2026-08-14.**
- `05-01` ✅ history fetch layer (react-native-svg 15.15.4, `historyCdnUrl` extra, `mobile/lib/history.ts`, 7 tests)
- `05-02` ✅ History tab UI (`HistoryStrip`, `ForecastCurve`, real `history.tsx`, 8 tests)
- `05-03` Task 1 ✅ eas.json `submit.production` profiles + app.json metadata; Gates A/B ⏳ human
- Summaries: `.planning/phases/05-polish-store-submission/05-01/02/03-SUMMARY.md`

## Human gates (no model can do these)

1. **Phase 5 Gate A — Google Play ($25)**: create Play Console app (`app.cocuyo.mobile`), place service-account JSON at `mobile/credentials/google-service-account.json` (now gitignored), `eas build --platform android --profile production` (needs `eas-cli` + `EXPO_ACCESS_TOKEN` or `expo login`), FIRST manual AAB upload to Internal testing, complete listing. Then `eas submit --platform android --profile production --latest` works. Type "android-live" when done.
2. **Phase 5 Gate B — iOS ($99/yr)**: enroll Apple Developer Program (blocked since Phase 1), create App Store Connect record, fill `ascAppId`/`appleTeamId` in eas.json, build + submit. Type "ios-review" or "ios-deferred".
3. **Phase 3**: physical-device push smoke test (4 UAT items, `03-HUMAN-UAT.md`).
4. **Phase 4**: on-device permission/delivery/cancel gates G1–G3, iOS G5.
5. **Contacts data**: `contacts.json` still has "Por verificar" placeholders for some states (CORPOELEC numbers need manual research).

## Known accepted stubs

- M-Lab collector stub (`pipeline/collector_mlab.py`) — approved decision #3, returns no signal.
- `pipeline/validation.py` device-fingerprint TODOs — deferred by design (ADR-005, `device_fingerprint: null`).

## Uncommitted (pre-existing, not mine — do not commit unless asked)

- `.planning/HANDOFF.json` (deleted), `.planning/config.json`, `app/tsconfig.tsbuildinfo`, `pipeline/__pycache__/*.pyc`, untracked `.planning/codebase/` + `skill-observations/`.
