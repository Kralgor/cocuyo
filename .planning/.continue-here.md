# Continue Here — Cocuyo Mobile

**Paused:** 2026-06-12 (context exhaustion at 100%)
**Milestone:** v1.0 — 1/5 phases complete (20%)
**HEAD:** `8308645` docs(02): plan revision

---

## Resume in one line

Run `/gsd:execute-phase 2` in a fresh session. Phase 2 is fully planned + verified; no code executors have run yet.

---

## Exact position

**Phase 2 (Reporting + Sharing + Quick Wins) — PLANNED & PLAN-CHECKER PASSED, NOT executed.**

All planning artifacts committed (clean state, nothing half-built):
- `02-CONTEXT.md`, `02-RESEARCH.md`, `02-VALIDATION.md`, `02-PATTERNS.md`
- `02-UI-SPEC.md` (approved, 3 advisory flags)
- 7 plans `02-01`..`02-07` across 4 waves
- plan-checker: **PASSED** after 1 revision round (commit `8308645`)

The last action attempted was `gsd-sdk query init.execute-phase 2` — it succeeded. Next step is spawning Wave 1.

---

## Wave map for execution

- **Wave 1** — `02-01` foundation. ⚠️ Contains a **BLOCKING human checkpoint**: you must run the parroquia SQL in the Supabase dashboard before reporting works:
  ```sql
  ALTER TABLE outage_reports ADD COLUMN parroquia TEXT;
  GRANT INSERT (parroquia) ON outage_reports TO anon;
  ```
  (Column-level GRANT does NOT auto-extend — the explicit GRANT is required, per 02-RESEARCH.md.)
- **Wave 2** — `02-02` test stubs + curated `parroquias.json` (Zulia + Distrito Capital hand-authored — dataset gap found in research) + `contacts.json`.
- **Wave 3** — `02-03` (reporting core: submitReport/queue/GPS), `02-04` (share.ts + parroquias lookup), `02-05` (AMOLED + useBattery + adaptive useStatus). Parallel, zero file overlap.
- **Wave 4** — `02-06` (report UI + SharePrompt), `02-07` (zone-tab quick wins). Parallel. Each ends in an **on-device human-verify checkpoint** — you build/run the app and confirm.

Models: executor=sonnet, verifier=sonnet. commit_docs=true. No worktrees (config workflow.use_worktrees unset).

---

## Key decisions locked this session (don't relitigate)

- **ADR-007**: anon key only in `app.json extra` (public by design). Never service_role.
- **ADR-005**: `device_fingerprint: null` in report payload until Phase 4.
- **Parroquia**: report-side tagging INCLUDED in Phase 2 (optional cascading picker). GPS→polygon auto-detect and scored-parroquia quorum DEFERRED (todo `.planning/todos/pending/2026-06-11-parroquia-level-reporting-hyperlocal.md`).
- **SharePrompt ownership**: created + rendered by `02-06` (report.tsx post-submit), NOT cross-tab. `02-07` ships only the hero share button. (This was the revision fix.)
- **Image share cards** (react-native-view-shot): stretch only, deferred.
- Live URLs: status `https://cocuyo.kralgor.com/status.json`, history `https://cocuyo.kralgor.com/history/`, web app `https://app.cocuyo.kralgor.com`. `cdn.cocuyo.app` is DEAD.

---

## Shipped earlier this session (all committed + deployed)

- Android preview APK built (EAS): https://expo.dev/artifacts/eas/VbWdmobV2j9bQlxapJHGEGOpigXCY-_gHiTA7ieVS0Y.apk
- Web deployed: desktop two-pane UI, Bajones honesty fix, dead-CDN fix, shell height fix
- History backfill now runs weekly in CI (verified fresh on R2)
- Pipeline: VIIRS h5py extraction + RIPE Atlas + M-Lab stub merged (536 tests pass)

---

## After Phase 2

Phases 3 (Push Notifications), 4 (Food Spoilage Timers), 5 (Polish + Store Submission) remain. The `/gsd:autonomous` flow continues through them. Phase 3 blocker noted: Expo Push Token receipt polling + iOS background-fetch breaking changes — research before planning.

## Open user tasks

1. Run the parroquia SQL above when Wave 1 checkpoint hits.
2. Per-state emergency contact numbers (`contacts.json`) need real values — currently "Por verificar" placeholders.
3. Test the Android APK + desktop site; report UI issues.
