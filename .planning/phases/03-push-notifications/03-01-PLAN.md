---
phase: 03-push-notifications
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_notify.py
  - mobile/jest.setup.js
  - mobile/__tests__/lib/notifications.test.ts
autonomous: true
requirements: [NOTF-01, NOTF-02, NOTF-04, INFR-01, INFR-02, INFR-03]
must_haves:
  truths:
    - "Running pytest on tests/test_notify.py fails with a collection/import error (module pipeline.notify does not exist yet)"
    - "Running jest on mobile/__tests__/lib/notifications.test.ts fails because mobile/lib/notifications.ts does not exist yet"
    - "Every Phase 3 requirement has at least one named failing test"
  artifacts:
    - path: tests/test_notify.py
      provides: "Offline failing test scaffold for pipeline fan-out, cooldown, adjacency, suppression"
      contains: "from pipeline.notify import send_notifications"
    - path: mobile/__tests__/lib/notifications.test.ts
      provides: "Failing test scaffold for token registration + registerToken"
      contains: "registerToken"
    - path: mobile/jest.setup.js
      provides: "expo-notifications + expo-device mocks for all notification tests"
      contains: "expo-notifications"
  key_links:
    - from: tests/test_notify.py
      to: pipeline.notify.send_notifications
      via: "import (will fail until plan 05)"
      pattern: "from pipeline.notify import"
    - from: mobile/__tests__/lib/notifications.test.ts
      to: mobile/lib/notifications.ts
      via: "import (will fail until plan 06)"
      pattern: "from .*lib/notifications"
---

<objective>
Create the failing test scaffolds (Nyquist Wave 0) that every later Phase 3 plan
verifies against. These tests define the executable contract for the pipeline
fan-out module and the mobile token-registration library BEFORE either is built.

Purpose: Each requirement (NOTF-01/02/04, INFR-01/02/03) gets a named automated
test now, so downstream plans have a concrete `<automated>` target rather than
"MISSING". Tests fail today (red) and turn green as plans 05 and 06 land.
Output: tests/test_notify.py, mobile/__tests__/lib/notifications.test.ts, and the
expo mocks in mobile/jest.setup.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-push-notifications/03-RESEARCH.md
@.planning/phases/03-push-notifications/03-PATTERNS.md
@.planning/phases/03-push-notifications/03-VALIDATION.md
@tests/test_outage_lifecycle.py
@mobile/__tests__/lib/api.test.ts
@mobile/jest.setup.js

<interfaces>
The contracts these tests assert against (built in plans 05 + 06):

pipeline/notify.py (plan 05):
  def send_notifications(lifecycle_result: dict, regions_scored: dict, client, now: datetime, cooldown_hours: int = 3) -> dict
    returns {"sent": int, "skipped_cooldown": int, "skipped_unstable": int}
  def _fetch_tokens(zones: list[str], event_type: str, client) -> list[str]
  def _is_suppressed(zone: str, event_type: str, cooldown_hours: int, now: datetime, client) -> bool
  def _send_expo_batch(messages: list[dict]) -> list[dict]   # patched in tests

mobile/lib/notifications.ts (plan 06):
  registerToken is re-exported from mobile/lib/api.ts; test imports from '@/lib/api'
  registerForPushNotificationsAsync(): Promise<string | null>

Lifecycle event shape (from process_lifecycle, RESEARCH lines 593-619):
  {"new_outages": ["caracas", ...], "restorations": ["maracaibo", ...]}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create failing pipeline fan-out test scaffold</name>
  <files>tests/test_notify.py</files>
  <read_first>
    - tests/test_outage_lifecycle.py (analog — MagicMock client builder lines 36-43, test class style, NOW constant)
    - .planning/phases/03-push-notifications/03-RESEARCH.md (Validation Architecture: Phase Requirements → Test Map lines 884-897; mock data lines 905-924; mock-client chain lines 707-720)
    - .planning/phases/03-push-notifications/03-PATTERNS.md (test_notify.py section: header, _mock_client fixture, test class names lines 681-766)
    - .planning/phases/03-push-notifications/03-CONTEXT.md (D-07 adjacency, D-09 cooldown, D-10 unstable suppression)
  </read_first>
  <action>
    Create tests/test_notify.py importing `from pipeline.notify import send_notifications, _is_suppressed, _fetch_tokens, _send_expo_batch` (import WILL fail until plan 05 — that is correct red state). Follow tests/test_outage_lifecycle.py style exactly: module docstring "All offline — mock Supabase and HTTP", `NOW = datetime(2026, 6, 13, 14, 0, 0, tzinfo=timezone.utc)`, helper builders `_lifecycle_result(new_outages, restorations)`, `_region_scored(status, score)`, and `_mock_client(tokens=None, suppressed=False)` using the chained-MagicMock pattern from PATTERNS lines 707-720 (push_tokens chain `.table().select().in_().eq().execute()`, notification_log chain `.table().select().eq().eq().gte().limit().execute()`, `.insert().execute()`).
    Implement these named test methods (map directly to RESEARCH Test Map so each requirement has a test):
    - class TestSendNotifications: test_outage_fires_for_subscribers (NOTF-01: asserts _send_expo_batch called once, message title contains "Sin luz", data.event_type == "outage"), test_restoration_fires_for_subscribers (NOTF-02: title contains "Volvió la luz", event_type "restoration"), test_neighbor_outage_fires_for_neighbor_subscribers (NOTF-04: outage in caracas fans to subscribers of los_teques/guarenas_guatire/maracay per ADJACENCY_MAP, event_type "neighbor_outage"), test_no_tokens_no_http_call (INFR-01: empty token list → _send_expo_batch not called, result["sent"]==0).
    - class TestExpoApi: test_message_format (INFR-01: each message dict has keys to/title/body/data/channelId), test_batch_chunking (INFR-01: 150 tokens → _send_expo_batch called with batches ≤100).
    - class TestSuppression: test_unstable_zone_skipped (NOTF-01/D-10: regions_scored status "unstable" → no send, result["skipped_unstable"]>=1).
    - class TestCooldown: test_cooldown_suppresses_repeat (INFR-03: _mock_client(suppressed=True) → no send, result["skipped_cooldown"]>=1), test_restoration_fires_after_outage_within_window (INFR-03: outage in log but restoration is different event_type → restoration still fires).
    - class TestEdgeCases: test_empty_lifecycle_events_no_send (INFR-03: empty new_outages+restorations → _send_expo_batch not called), test_all_signals_none_no_crash (send_notifications with minimal regions_scored does not raise).
    Patch `pipeline.notify._send_expo_batch` with `unittest.mock.patch` in tests that assert send behavior so no real HTTP is attempted. Do NOT mark tests xfail/skip — they must error on the missing import (red) until plan 05.
  </action>
  <verify>
    <automated>python -m pytest tests/test_notify.py -q 2>&1 | grep -Eq "ModuleNotFoundError|ImportError|No module named 'pipeline.notify'"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_notify.py contains `from pipeline.notify import send_notifications`
    - File defines class TestSendNotifications, TestExpoApi, TestSuppression, TestCooldown, TestEdgeCases
    - `python -m pytest tests/test_notify.py -q` exits non-zero with a collection/import error referencing pipeline.notify (red, as expected pre-plan-05)
    - No test is decorated with @pytest.mark.skip or @pytest.mark.xfail
    - No real network call: all send tests patch pipeline.notify._send_expo_batch
  </acceptance_criteria>
  <done>tests/test_notify.py exists, names a test per Phase 3 requirement, and fails only because pipeline.notify is not yet implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Add expo mocks to jest.setup.js and create failing mobile registration test</name>
  <files>mobile/jest.setup.js, mobile/__tests__/lib/notifications.test.ts</files>
  <read_first>
    - mobile/jest.setup.js (existing mocks — add expo-notifications + expo-device alongside)
    - mobile/__tests__/lib/api.test.ts (analog — fetch mocking, never-throw assertion style)
    - .planning/phases/03-push-notifications/03-RESEARCH.md (expo-notifications jest mock lines 957-973; INFR-02 test rows lines 893-894; registerToken pattern lines 405-433)
    - .planning/phases/03-push-notifications/03-PATTERNS.md (registerToken never-throw shape lines 414-447)
  </read_first>
  <action>
    In mobile/jest.setup.js add a `jest.mock('expo-notifications', ...)` returning stubs: setNotificationHandler (jest.fn), setNotificationChannelAsync (resolves null), getPermissionsAsync (resolves {status:'granted'}), requestPermissionsAsync (resolves {status:'granted'}), getExpoPushTokenAsync (resolves {data:'ExponentPushToken[test123]'}), AndroidImportance ({HIGH:4, MAX:5}), addNotificationReceivedListener and addNotificationResponseReceivedListener (return {remove: jest.fn()}). Add `jest.mock('expo-device', () => ({ isDevice: true }))` (if expo-device is already mocked, extend rather than duplicate).
    Create mobile/__tests__/lib/notifications.test.ts importing `{ registerToken }` from '@/lib/api' (registerToken does not exist yet → red until plan 04 adds it; this test also covers plan 06). Use the global.fetch mock pattern from api.test.ts. Tests: "registerToken returns {ok:true, offline:false} on 201" (mock fetch ok:true), "registerToken returns {ok:false, offline:false} on non-OK response" (mock fetch ok:false), "registerToken returns {ok:false, offline:true} on network throw" (mock fetch rejects), and "registerToken POSTs to /rest/v1/push_tokens with Prefer merge-duplicates header" (assert fetch called with URL containing /rest/v1/push_tokens and Prefer header containing resolution=merge-duplicates).
    Do NOT skip/xfail. Red state is the missing registerToken export.
  </action>
  <verify>
    <automated>cd mobile && npx jest __tests__/lib/notifications.test.ts --watchAll=false 2>&1 | grep -Eq "Cannot find|is not a function|registerToken|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - mobile/jest.setup.js contains a jest.mock for 'expo-notifications' exposing getExpoPushTokenAsync and setNotificationChannelAsync
    - mobile/jest.setup.js mocks 'expo-device' with isDevice: true
    - mobile/__tests__/lib/notifications.test.ts imports registerToken and asserts the {ok, offline} contract for 201 / non-OK / network-throw
    - `cd mobile && npx jest __tests__/lib/notifications.test.ts --watchAll=false` fails (red) referencing missing registerToken
    - No --watch flags; uses --watchAll=false
  </acceptance_criteria>
  <done>jest.setup.js mocks the expo SDK; notifications.test.ts asserts the registerToken never-throw contract and fails only because registerToken is not yet exported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → none | This plan only creates test files; no runtime surface, no external input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Tests issue real network calls to Expo/Supabase during CI | mitigate | All HTTP mocked: pipeline tests patch pipeline.notify._send_expo_batch; mobile tests mock global.fetch and expo-notifications. No live endpoints contacted. |
| T-03-SC | Tampering | npm/pip installs | accept | No package installs in this plan (test files only). Package legitimacy handled in plans 04 + 02. Audit table present in RESEARCH.md. |
</threat_model>

<verification>
- `python -m pytest tests/test_notify.py -q` → red with ModuleNotFoundError for pipeline.notify
- `cd mobile && npx jest __tests__/lib/notifications.test.ts --watchAll=false` → red (missing registerToken)
- Existing suites unaffected: `cd mobile && npx jest --watchAll=false` does not break on the new jest.setup mocks
</verification>

<success_criteria>
Every Phase 3 requirement (NOTF-01/02/04, INFR-01/02/03) has a named, offline,
non-skipped test that currently fails only because the implementation does not
yet exist. expo SDK is mocked in jest.setup.js.
</success_criteria>

<output>
Create `.planning/phases/03-push-notifications/03-01-SUMMARY.md` when done
</output>
