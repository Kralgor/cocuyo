---
phase: 02-reporting-sharing-quick-wins
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - mobile/constants/colors.ts
  - mobile/contexts/ThemeContext.tsx
  - mobile/hooks/useBattery.ts
  - mobile/hooks/useReportQueue.ts
  - mobile/hooks/useStatus.ts
autonomous: true
requirements: [BATT-01, BATT-02, REPT-03]

must_haves:
  truths:
    - "AMOLED theme variant selectable; bg #000000, panel #0A0A0A, all other tokens inherit dark"
    - "useBattery reports isBatterySaving when level < 0.20 (ignores -1 unavailable)"
    - "useStatus accepts a refreshInterval so battery-saving stretches polling 10min→30min"
    - "useReportQueue flushes the offline queue on connectivity-return and app foreground"
  artifacts:
    - path: "mobile/constants/colors.ts"
      provides: "AMOLED_THEME constant"
      contains: "AMOLED_THEME"
    - path: "mobile/contexts/ThemeContext.tsx"
      provides: "'amoled' override type + AMOLED_THEME branch"
      contains: "amoled"
    - path: "mobile/hooks/useBattery.ts"
      provides: "useBattery() → { isBatterySaving, level }"
      exports: ["useBattery"]
    - path: "mobile/hooks/useReportQueue.ts"
      provides: "useReportQueue() → { queueLength, isFlushing }"
      exports: ["useReportQueue"]
  key_links:
    - from: "mobile/contexts/ThemeContext.tsx"
      to: "AMOLED_THEME"
      via: "effective === 'amoled' branch"
      pattern: "AMOLED_THEME"
    - from: "mobile/hooks/useReportQueue.ts"
      to: "flushQueue + submitReport"
      via: "NetInfo + AppState listeners"
      pattern: "flushQueue"
---

<objective>
Build the battery + theme + queue-sync layer: the AMOLED theme variant (colors +
ThemeContext), the useBattery hook (BATT-02 threshold), the useStatus interval
extension (battery-adaptive polling), and useReportQueue (NetInfo + AppState flush
triggers). Turns the amoled Wave-0 test green and supplies the hooks the screens mount.

Purpose: BATT-01 AMOLED, BATT-02 low-battery throttle, REPT-03 auto-sync triggers.
Output: colors.ts (AMOLED), ThemeContext (amoled branch), useBattery, useReportQueue, useStatus (extended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-reporting-sharing-quick-wins/02-RESEARCH.md
@.planning/phases/02-reporting-sharing-quick-wins/02-PATTERNS.md

<interfaces>
From mobile/constants/colors.ts (existing — add AMOLED_THEME after DARK_THEME):
```typescript
export interface MobileTheme { bg; panel; ink; inkDim; inkFaint; accent; ok; warn; danger; line; lineStrong; }
export const DARK_THEME: MobileTheme = { bg:'#0F0F0F', panel:'#1A1A1A', /* ... */ };
```
From mobile/contexts/ThemeContext.tsx (existing — extend union 'light'|'dark'|null → add 'amoled'):
```typescript
interface ThemeContextValue { theme; override: 'light'|'dark'|null; setOverride: (v:'light'|'dark'|null)=>void; }
const storedOverride = storage.getString(STORAGE_KEYS.themeOverride) as 'light'|'dark'|null ?? null;
const theme = effective === 'light' ? LIGHT_THEME : DARK_THEME;
```
From mobile/hooks/useStatus.ts (existing — add optional refreshInterval param → refetchInterval):
```typescript
export function useStatus(): { data; isLoading; isError; refetch };
const query = useQuery({ queryKey:['status'], queryFn: async () => {...} });
```
From mobile/lib/queue.ts (02-03): flushQueue(submitFn), getQueue
From mobile/lib/api.ts (02-03): submitReport
expo-battery: getBatteryLevelAsync()→0..1 or -1; addBatteryLevelListener(cb)→{remove}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AMOLED_THEME and extend ThemeContext for 'amoled'</name>
  <read_first>
    - mobile/constants/colors.ts (full — add after DARK_THEME)
    - mobile/contexts/ThemeContext.tsx (full — atomic union + branch update)
    - mobile/lib/storage.ts (themeOverride comment already 'amoled' after 02-01)
    - mobile/__tests__/lib/amoled.test.ts (AMOLED_THEME assertions)
    - .planning/phases/02-reporting-sharing-quick-wins/02-RESEARCH.md (Pattern 6 AMOLED + Pitfall 7)
    - .planning/phases/02-reporting-sharing-quick-wins/02-UI-SPEC.md (AMOLED Theme table; AMOLED Flow)
  </read_first>
  <files>mobile/constants/colors.ts, mobile/contexts/ThemeContext.tsx</files>
  <action>
    In colors.ts, after DARK_THEME, add `export const AMOLED_THEME: MobileTheme = { ...DARK_THEME, bg: '#000000', panel: '#0A0A0A' };` with the section-divider comment. In ThemeContext.tsx, atomically widen every `'light' | 'dark' | null` occurrence to `'light' | 'dark' | 'amoled' | null` — the interface `override` + `setOverride` param, the `storedOverride` cast, and the useState generic (Pitfall 7: stale 'amoled' otherwise falls through to null). Import AMOLED_THEME. Replace the theme selection with: `const theme = effective === 'light' ? LIGHT_THEME : effective === 'amoled' ? AMOLED_THEME : DARK_THEME;`. Preserve the existing MMKV remove() null-path and its comment unchanged.
  </action>
  <verify>
    <automated>cd mobile && npx jest --testPathPattern="__tests__/lib/amoled" 2>&1 | tail -6 && node -e "const c=require('fs').readFileSync('contexts/ThemeContext.tsx','utf8'); if((c.match(/'amoled'/g)||[]).length<3)throw new Error('amoled not widened everywhere'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - colors.ts exports AMOLED_THEME with bg '#000000', panel '#0A0A0A', other tokens spread from DARK_THEME
    - ThemeContext override type is 'light' | 'dark' | 'amoled' | null in interface, setOverride, storedOverride cast, and useState generic (>=3 'amoled' occurrences)
    - theme selection resolves AMOLED_THEME when effective === 'amoled'
    - MMKV null-path remove() comment preserved
    - amoled test (theme portion) GREEN
  </acceptance_criteria>
  <done>AMOLED variant available; ThemeContext selects it without the Pitfall-7 fallthrough.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useBattery, useStatus interval extension, useReportQueue</name>
  <read_first>
    - mobile/AGENTS.md (Expo SDK 56 docs — verify expo-battery + AppState APIs)
    - mobile/hooks/useOffline.ts (listener-hook + pure-helper pattern)
    - mobile/hooks/useStatus.ts (full — add optional param)
    - mobile/__tests__/lib/amoled.test.ts (useBattery assertions)
    - .planning/phases/02-reporting-sharing-quick-wins/02-RESEARCH.md (Pattern 3 queue flush hook, Pattern 4 battery listener; Pitfall 2 isInternetReachable)
    - .planning/phases/02-reporting-sharing-quick-wins/02-PATTERNS.md (useBattery / useReportQueue / useStatus sections)
  </read_first>
  <files>mobile/hooks/useBattery.ts, mobile/hooks/useReportQueue.ts, mobile/hooks/useStatus.ts</files>
  <action>
    Create `mobile/hooks/useBattery.ts`: useState level=1.0; useEffect calls getBatteryLevelAsync (set if !== -1) then addBatteryLevelListener (set on change if !== -1); cleanup sub.remove(); return `{ isBatterySaving: level < 0.20 && level !== -1, level }` (RESEARCH Pattern 4). Extend `mobile/hooks/useStatus.ts`: add optional `refreshInterval?: number` param; when provided, pass it as `refetchInterval` to useQuery; default undefined preserves existing behavior (no other behavior change). Create `mobile/hooks/useReportQueue.ts`: import NetInfo + AppState, flushQueue from '@/lib/queue', submitReport from '@/lib/api', getQueue. useState queueLength + isFlushing. useEffect mounts (a) NetInfo.addEventListener → if `state.isConnected && state.isInternetReachable === true` (strict, Pitfall 2) call a guarded flush that sets isFlushing, awaits flushQueue(submitReport), refreshes queueLength; (b) AppState 'change'→'active' → NetInfo.fetch() then same guarded flush. Cleanup unsub + sub.remove(). Return `{ queueLength, isFlushing }`. Mount once (report screen or layout) — document that in a comment.
  </action>
  <verify>
    <automated>cd mobile && npx jest --testPathPattern="__tests__/lib/amoled" 2>&1 | tail -6 && node -e "const s=require('fs').readFileSync('hooks/useStatus.ts','utf8'); if(s.indexOf('refreshInterval')<0||s.indexOf('refetchInterval')<0)throw new Error('useStatus interval missing'); const q=require('fs').readFileSync('hooks/useReportQueue.ts','utf8'); if(q.indexOf('isInternetReachable')<0)throw new Error('reachable guard missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - useBattery returns isBatterySaving=true at level 0.10, false at level -1 (amoled test GREEN)
    - useStatus accepts refreshInterval and maps it to refetchInterval; omitting it preserves Phase 1 behavior
    - useReportQueue gates flush on `isConnected && isInternetReachable === true` (strict equality)
    - useReportQueue exports { queueLength, isFlushing } and calls flushQueue(submitReport) on connectivity-return AND app-foreground
    - amoled test (battery portion) GREEN
  </acceptance_criteria>
  <done>Battery hook, adaptive polling, and dual-trigger queue sync implemented and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| connectivity event → queue flush | captive-portal false positive can fire flush |
| battery sensor → app | level read transiently, no storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | queue flush on captive portal | mitigate | Strict `isInternetReachable === true` guard (Pitfall 2) before any POST |
| T-02-12 | Denial of Service | over-frequent polling drains battery | mitigate | BATT-02 stretches refetchInterval to 30min under 20% — reduces network/battery load |
</threat_model>

<verification>
- `cd mobile && npx jest --testPathPattern="__tests__/lib/amoled"` GREEN
- useReportQueue flush guarded by isInternetReachable === true; useStatus interval param present
</verification>

<success_criteria>
AMOLED theme, battery-aware polling, and offline-queue auto-sync hooks complete and tested; ready for screen wiring.
</success_criteria>

<output>
Create `.planning/phases/02-reporting-sharing-quick-wins/02-05-SUMMARY.md` when done
</output>
