---
phase: 02-reporting-sharing-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/package.json
  - mobile/app.json
  - mobile/jest.setup.js
  - mobile/lib/storage.ts
  - mobile/lib/i18n.ts
  - docs/schema.sql
  - docs/ARCHITECTURE.md
autonomous: false
requirements: [REPT-01, REPT-02, REPT-03, SHAR-01, BATT-01, BATT-02, BATT-03]
user_setup:
  - service: supabase
    why: "parroquia column + INSERT grant must exist before mobile reports include parroquia; mobile degrades gracefully (omits field) until applied"
    dashboard_config:
      - task: "Run ALTER TABLE outage_reports ADD COLUMN IF NOT EXISTS parroquia TEXT; then GRANT INSERT (parroquia) ON outage_reports TO anon;"
        location: "Supabase Dashboard → SQL Editor"
    env_vars:
      - name: supabaseUrl
        source: "Supabase Dashboard → Project Settings → API → Project URL (added to app.json extra)"
      - name: supabaseAnonKey
        source: "Supabase Dashboard → Project Settings → API → anon/public key (added to app.json extra, public by design — ADR-007)"

must_haves:
  truths:
    - "expo-location, expo-battery, expo-crypto installed at SDK 56 versions"
    - "Supabase anon key + url readable via Constants.expoConfig.extra (anon only — ADR-007)"
    - "jest runs with expo-location/expo-battery/expo-crypto mocks; existing Phase 1 tests still green"
    - "parroquia column documented in schema.sql + ARCHITECTURE.md; SQL applied in Supabase (user checkpoint)"
  artifacts:
    - path: "mobile/app.json"
      provides: "supabaseUrl/supabaseAnonKey in extra + expo-location plugin entry"
      contains: "supabaseAnonKey"
    - path: "mobile/jest.setup.js"
      provides: "expo-location, expo-battery, expo-crypto jest mocks"
      contains: "expo-battery"
    - path: "mobile/lib/storage.ts"
      provides: "reportQueue + lastReportTime STORAGE_KEYS; themeOverride amoled comment"
      contains: "reportQueue"
    - path: "docs/schema.sql"
      provides: "parroquia ALTER TABLE + GRANT migration"
      contains: "parroquia"
  key_links:
    - from: "mobile/lib/api.ts (Wave 1)"
      to: "Constants.expoConfig.extra.supabaseAnonKey"
      via: "expo-constants read"
      pattern: "supabaseAnonKey"
---

<objective>
Lay the Phase 2 foundation: install the three new Expo packages, wire Supabase
anon credentials into app.json (ADR-007 — anon key only), extend storage keys,
add i18n strings, document and gate the additive `parroquia` schema change,
and add jest mocks. No feature logic — this plan exists so every Wave 1+ plan
has its contracts and credentials in place.

Purpose: Unblock parallel feature work without scavenger hunts or missing deps.
Output: Installed deps, configured app.json, extended storage/i18n, schema doc + applied migration, jest mocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reporting-sharing-quick-wins/02-RESEARCH.md
@.planning/phases/02-reporting-sharing-quick-wins/02-PATTERNS.md

<interfaces>
From mobile/lib/storage.ts (existing — extend, do not rewrite):
```typescript
export const storage = createMMKV({ id: 'cocuyo' });
export const STORAGE_KEYS = {
  hasSeenOnboarding: 'hasSeenOnboarding',
  selectedZone:      'selectedZone',
  themeOverride:     'themeOverride',   // 'light' | 'dark' | null
  cacheTimestamp:    'statusCacheTimestamp',
} as const;
```

From mobile/app.json extra (existing — append keys, do not replace):
```json
"extra": { "statusCdnUrl": "https://cocuyo.kralgor.com/status.json", "router": {}, "eas": {...} }
```

From mobile/app.json plugins (existing array — append expo-location entry):
already contains: "expo-router", "expo-splash-screen", "expo-localization", "expo-build-properties"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps + configure app.json (Supabase extra + expo-location plugin)</name>
  <read_first>
    - mobile/AGENTS.md (read https://docs.expo.dev/versions/v56.0.0/ before writing code)
    - mobile/app.json (full file — append only, preserve existing keys)
    - mobile/package.json
    - .planning/phases/02-reporting-sharing-quick-wins/02-RESEARCH.md (Standard Stack + expo-location plugin entry + Open Question 4)
  </read_first>
  <files>mobile/package.json, mobile/app.json</files>
  <action>
    Run `cd mobile && npx expo install expo-location expo-battery expo-crypto` to add expo-location (~56.0.17), expo-battery (~56.0.4), expo-crypto (~56.0.4). Verify @react-native-community/netinfo (12.0.1) and expo-linking already present; do NOT reinstall if present. In app.json `extra`, ADD two keys alongside statusCdnUrl: `supabaseUrl` and `supabaseAnonKey`. Set them to the live Supabase project URL and anon/public key (anon key is public by design per ADR-007 — plain app.json extra is acceptable; NEVER add service_role key). If the executor does not have the real values, set placeholder strings and surface them in the user_setup checkpoint of Task 3. In app.json `plugins` array, ADD the expo-location plugin tuple with `locationWhenInUsePermission` set to the Spanish copy "Cocuyo usa tu ubicación para detectar tu zona automáticamente." Do not remove or reorder existing plugins. Do NOT add react-native-view-shot (deferred/stretch).
  </action>
  <verify>
    <automated>cd mobile && node -e "const p=require('./package.json').dependencies; ['expo-location','expo-battery','expo-crypto'].forEach(d=>{if(!p[d])throw new Error('missing '+d)}); const a=require('./app.json').expo; if(!a.extra.supabaseAnonKey)throw new Error('no supabaseAnonKey'); if(!a.extra.supabaseUrl)throw new Error('no supabaseUrl'); if(JSON.stringify(a.plugins).indexOf('expo-location')<0)throw new Error('no expo-location plugin'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `mobile/package.json` dependencies include expo-location, expo-battery, expo-crypto
    - `app.json` extra contains `supabaseUrl` AND `supabaseAnonKey` (string values)
    - `app.json` extra does NOT contain any key matching /service.?role/i
    - `app.json` plugins array contains an expo-location tuple with `locationWhenInUsePermission` in Spanish
    - Existing extra keys (statusCdnUrl, router, eas) and existing plugins preserved
  </acceptance_criteria>
  <done>Three deps installed; app.json carries anon Supabase creds + expo-location plugin; no service_role key anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Extend storage keys, i18n strings, and jest mocks</name>
  <read_first>
    - mobile/lib/storage.ts (full file)
    - mobile/lib/i18n.ts (string-table structure + tt() signature)
    - mobile/jest.setup.js (existing mock style)
    - .planning/phases/02-reporting-sharing-quick-wins/02-UI-SPEC.md (Copywriting Contract — exact ES/EN strings)
    - .planning/phases/02-reporting-sharing-quick-wins/02-RESEARCH.md (Wave 0 jest.setup.js additions block)
  </read_first>
  <files>mobile/lib/storage.ts, mobile/lib/i18n.ts, mobile/jest.setup.js</files>
  <action>
    In `mobile/lib/storage.ts` STORAGE_KEYS, append `reportQueue: 'reportQueue'` (JSON-serialized QueuedReport[]) and `lastReportTime: 'lastReportTime'` (epoch ms of last enqueue, 30-min dedup). Update the `themeOverride` inline comment to `'light' | 'dark' | 'amoled' | null`. In `mobile/lib/i18n.ts`, add all Phase 2 ES/EN keys from the UI-SPEC Copywriting Contract: report_title, gps_resolving, gps_resolved, gps_manual, report_out, report_back, cooldown_notice, queue_pending_header, queue_pending_body, confirm_title, zone_label, parroquia_label, submit_report, toast_sent, toast_queued, toast_synced, toast_failed, contacts_header, contacts_unverified, contacts_a11y, share_prompt_heading, share_prompt_body, share_prompt_cta, battery_saving_banner, battery_banner_a11y, theme_amoled. Use exact UI-SPEC wording (placeholders like {N}, {name} preserved). In `mobile/jest.setup.js`, append the three new jest.mock blocks for expo-location (requestForegroundPermissionsAsync→granted, getCurrentPositionAsync→Caracas coords, Accuracy.Balanced=3), expo-battery (getBatteryLevelAsync→0.85, addBatteryLevelListener→{remove}), and expo-crypto (randomUUID→'mock-uuid-1234'), matching existing mock style.
  </action>
  <verify>
    <automated>cd mobile && npx jest --passWithNoTests 2>&1 | tail -5 && node -e "const s=require('fs').readFileSync('lib/storage.ts','utf8'); if(s.indexOf('reportQueue')<0||s.indexOf('lastReportTime')<0)throw new Error('keys missing'); const j=require('fs').readFileSync('jest.setup.js','utf8'); ['expo-location','expo-battery','expo-crypto'].forEach(m=>{if(j.indexOf(m)<0)throw new Error('mock '+m)}); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - STORAGE_KEYS contains `reportQueue` and `lastReportTime`
    - themeOverride comment reads `'light' | 'dark' | 'amoled' | null`
    - i18n.ts contains all 26 Phase 2 keys with both `es` and `en` entries
    - jest.setup.js mocks expo-location, expo-battery, expo-crypto
    - All existing Phase 1 jest tests still pass (api/i18n/regionFilter/statusColor/storage)
  </acceptance_criteria>
  <done>Storage keys + i18n strings + jest mocks ready; full Phase 1 suite green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Apply parroquia schema migration in Supabase</name>
  <what-built>
    Documented the additive `parroquia` column in docs/schema.sql and docs/ARCHITECTURE.md.
  </what-built>
  <read_first>
    - docs/schema.sql (existing outage_reports DDL + GRANT pattern)
    - docs/ARCHITECTURE.md (schema/data-flow section to update)
    - .planning/phases/02-reporting-sharing-quick-wins/02-RESEARCH.md (Schema Change section + Pitfall 3)
  </read_first>
  <action>
    Before the checkpoint: append to `docs/schema.sql` the idempotent migration —
    `ALTER TABLE outage_reports ADD COLUMN IF NOT EXISTS parroquia TEXT;` followed by
    `GRANT INSERT (parroquia) ON outage_reports TO anon;` — and update `docs/ARCHITECTURE.md`
    to document the new nullable column (project rule: never modify schema without updating ARCHITECTURE.md).
    The RLS `anon_can_insert` policy is unchanged (WITH CHECK true); the column-level GRANT is what
    permits anon to write the new column. Column GRANTs do NOT auto-extend to new columns — the GRANT line is required.
  </action>
  <how-to-verify>
    1. Open Supabase Dashboard → SQL Editor for the Cocuyo project.
    2. Run: `ALTER TABLE outage_reports ADD COLUMN IF NOT EXISTS parroquia TEXT;`
    3. Run: `GRANT INSERT (parroquia) ON outage_reports TO anon;`
    4. Confirm column exists: `SELECT column_name FROM information_schema.columns WHERE table_name='outage_reports' AND column_name='parroquia';` returns one row.
    5. (Optional) Test an anon INSERT with a parroquia value and confirm the column is populated (not NULL) — this proves the GRANT took.
    6. Confirm app.json `extra.supabaseUrl` and `extra.supabaseAnonKey` hold the real project values (replace any placeholders from Task 1).
  </how-to-verify>
  <resume-signal>Type "applied" once the column + GRANT exist in Supabase and app.json holds real anon creds, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile app → Supabase REST | anon-key INSERT crosses here; only anon creds in client |
| build config (app.json extra) | anon key is public by design (ADR-007) |
| npm install | new package code enters the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Supabase creds in app.json | mitigate | Only anon key + url in extra; automated check rejects any /service.?role/i key (Task 1 verify) |
| T-02-02 | Tampering | parroquia column GRANT | mitigate | Column-level GRANT INSERT(parroquia) TO anon only; RLS WITH CHECK(true) unchanged; no service_role exposure |
| T-02-SC | Tampering | npm installs (expo-location/battery/crypto) | mitigate | All three are Expo-official org packages (RESEARCH Package Legitimacy Audit: Approved); no [ASSUMED]/[SUS] — no blocking gate needed |
</threat_model>

<verification>
- `cd mobile && npx jest --passWithNoTests` green (Phase 1 suite intact + new mocks load)
- app.json carries anon Supabase creds + expo-location plugin; no service_role key
- docs/schema.sql + docs/ARCHITECTURE.md document parroquia; SQL applied in Supabase (checkpoint)
</verification>

<success_criteria>
Three deps installed, anon creds wired, storage/i18n/mocks extended, parroquia documented and applied. Wave 1 plans can read all contracts without exploration.
</success_criteria>

<output>
Create `.planning/phases/02-reporting-sharing-quick-wins/02-01-SUMMARY.md` when done
</output>
