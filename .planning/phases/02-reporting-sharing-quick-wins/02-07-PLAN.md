---
phase: 02-reporting-sharing-quick-wins
plan: 07
type: execute
wave: 4
depends_on: ["02-04", "02-05"]
files_modified:
  - mobile/components/ContactsCard.tsx
  - mobile/components/BatteryBanner.tsx
  - mobile/app/(tabs)/index.tsx
  - mobile/components/SettingsModal.tsx
autonomous: false
requirements: [SHAR-01, BATT-01, BATT-02, BATT-03]

must_haves:
  truths:
    - "Zone hero has a one-tap WhatsApp share button producing pre-formatted Spanish text"
    - "Números útiles card lists emergency contacts for the zone's state; tap opens dialer"
    - "Settings offers an AMOLED option that flips the app to true-black live"
    - "Below 20% battery, refresh stretches to 30min, animations pause, and a banner appears"
  artifacts:
    - path: "mobile/components/ContactsCard.tsx"
      provides: "Números útiles card with tel: rows"
      min_lines: 50
    - path: "mobile/components/BatteryBanner.tsx"
      provides: "Modo ahorro banner, session-dismiss"
      min_lines: 40
    - path: "mobile/app/(tabs)/index.tsx"
      provides: "share button + contacts card + battery banner + adaptive interval wiring"
  key_links:
    - from: "mobile/app/(tabs)/index.tsx"
      to: "shareToWhatsApp(composeShareText(...))"
      via: "hero share button"
      pattern: "shareToWhatsApp"
    - from: "mobile/app/(tabs)/index.tsx"
      to: "useBattery + useStatus(refreshInterval)"
      via: "battery-adaptive polling"
      pattern: "useBattery"
    - from: "mobile/components/SettingsModal.tsx"
      to: "setOverride('amoled')"
      via: "Apariencia 4th option"
      pattern: "amoled"
---

<objective>
Deliver the zone-screen quick wins: a WhatsApp share button on the hero, the
"Números útiles" contacts card, the AMOLED settings
option, and the battery-saving banner + adaptive polling wiring. Completes
SHAR-01, BATT-01/02/03.

Purpose: Sharing, emergency contacts, AMOLED, and low-battery conservation on the zone tab + settings.
NOTE: the post-report "Avisa a tus vecinos" SharePrompt is OWNED by plan 02-06
(created there, rendered in report.tsx post-submit). This plan does NOT create
or render SharePrompt — it ships only the standalone hero share button.
Output: ContactsCard.tsx, BatteryBanner.tsx, index.tsx (extended), SettingsModal.tsx (AMOLED option).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-reporting-sharing-quick-wins/02-PATTERNS.md
@.planning/phases/02-reporting-sharing-quick-wins/02-UI-SPEC.md

<interfaces>
Libs/hooks available (Wave 3):
- mobile/lib/share.ts: composeShareText(region, regionKey, lang), shareToWhatsApp(text)
- mobile/hooks/useBattery.ts: useBattery():{ isBatterySaving, level }
- mobile/hooks/useStatus.ts: useStatus(refreshInterval?) → refetchInterval
- mobile/contexts/ThemeContext.tsx: useTheme() → { theme, override:'light'|'dark'|'amoled'|null, setOverride }
- mobile/assets/contacts.json: { state, entries:[{name,number,verified}] }[]
Existing:
- mobile/app/(tabs)/index.tsx (zone screen; gear icon header Pressable pattern; StatusHero; SkeletonCard Animated.loop)
- mobile/components/SignalCard.tsx (card panel createStyles pattern)
- mobile/components/SettingsModal.tsx (Apariencia segmented control: Seguir sistema / Claro / Oscuro)
- mobile/components/StaleBanner.tsx (40dp warn banner)
- mobile/lib/regions.ts: REGIONS[key].state; mobile/lib/i18n.ts: tt
- @expo/vector-icons Ionicons; Linking from react-native
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContactsCard.tsx + BatteryBanner.tsx</name>
  <read_first>
    - mobile/AGENTS.md (Expo SDK 56 docs)
    - mobile/components/SignalCard.tsx (card panel + createStyles)
    - mobile/components/StaleBanner.tsx (40dp banner; accessibilityLiveRegion)
    - mobile/components/SettingsModal.tsx (Linking.openURL pattern; Pressable row + icon)
    - mobile/lib/share.ts (shareToWhatsApp), mobile/lib/regions.ts (state lookup)
    - mobile/assets/contacts.json (shape)
    - .planning/phases/02-reporting-sharing-quick-wins/02-UI-SPEC.md (Números útiles card; Battery Savings Banner; Copywriting)
    - .planning/phases/02-reporting-sharing-quick-wins/02-PATTERNS.md (ContactsCard / SharePrompt / BatteryBanner sections)
  </read_first>
  <files>mobile/components/ContactsCard.tsx, mobile/components/BatteryBanner.tsx</files>
  <action>
    Create `mobile/components/ContactsCard.tsx`: props `{ regionKey }`. Resolve REGIONS[regionKey].state, find contacts.json national + matching-state entries; if none, render null (no empty state). "Números útiles" section header (Label 13sp inkDim uppercase). panel card, line dividers, 48dp rows: Ionicons call-outline (accent) + name (Body 16sp ink) + number (Label 13sp inkDim); unverified entries append "Por verificar" suffix (warn, italic). Tap row → Linking.openURL(`tel:${number}`); skip tap when number empty. accessibilityRole="button", label "{name}: {number}. Toca para llamar." (SharePrompt.tsx is created by plan 02-06 — do NOT create it here.) Create `mobile/components/BatteryBanner.tsx`: copy StaleBanner structure; props `{ onDismiss }`; warn bg, 40dp, Ionicons battery-charging-outline + "Modo ahorro activo — actualización reducida" (Label 13sp ink); wrap in Pressable onPress=onDismiss; accessibilityLabel "Modo ahorro de batería activo. Toca para descartar." All strings via tt; styles via createStyles(theme).
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "components/(ContactsCard|BatteryBanner)" || echo "no type errors in new components"</automated>
  </verify>
  <acceptance_criteria>
    - ContactsCard renders null when no contacts for the state; otherwise tel: rows with accent call icon and "Por verificar" suffix on unverified entries
    - ContactsCard tap calls Linking.openURL with a tel: URL and skips empty numbers
    - BatteryBanner is a 40dp warn Pressable strip dismissable via onDismiss with the correct a11y label
    - No hardcoded Spanish strings (all via tt); styles via createStyles(theme)
    - `npx tsc --noEmit` reports no errors in the two files
  </acceptance_criteria>
  <done>Contacts card and battery banner built per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Extend index.tsx (share button, contacts, prompt, battery) + SettingsModal AMOLED option</name>
  <read_first>
    - mobile/app/(tabs)/index.tsx (full — gear header Pressable; StatusHero; ScrollView content order; SkeletonCard Animated.loop)
    - mobile/components/SettingsModal.tsx (full — Apariencia segmented control to extend to 4 options/2 rows)
    - mobile/hooks/useBattery.ts, mobile/hooks/useStatus.ts (interval param)
    - mobile/lib/share.ts (composeShareText/shareToWhatsApp)
    - .planning/phases/02-reporting-sharing-quick-wins/02-UI-SPEC.md (Zone Detail additions A–C; Settings AMOLED; Battery Mode Flow; AMOLED Flow)
    - .planning/phases/02-reporting-sharing-quick-wins/02-PATTERNS.md (index.tsx EXTEND section)
  </read_first>
  <files>mobile/app/(tabs)/index.tsx, mobile/components/SettingsModal.tsx</files>
  <action>
    In `mobile/app/(tabs)/index.tsx`: (1) add a share button at the StatusHero top-right (Ionicons share-social-outline 20dp, 44dp target via hitSlop, shown when region data present) → onPress shareToWhatsApp(composeShareText(region, regionKey, lang)). (2) Mount useBattery(); REPLACE the existing no-arg `useStatus()` destructure call in index.tsx (keep all destructured fields) with `useStatus(isBatterySaving ? 1_800_000 : 600_000)` — exactly one useStatus mount must remain (two mounts on the same query key double-fetch). (3) Render BatteryBanner (session-dismiss state, no MMKV) above content when isBatterySaving && !bannerDismissed — battery banner takes precedence over the staleness banner (show only one). Pause shimmer under battery saving: add prop `isSaving: boolean` to the SkeletonCard component inside index.tsx and pass `isBatterySaving`; inside SkeletonCard's useEffect, when isSaving is true do not start the Animated.loop (and stop the local `anim` in the effect cleanup / when isSaving flips true) — the animation is controlled INTERNALLY via the prop, never reached from the parent. (4) Render ContactsCard regionKey={...} at the bottom of the ScrollView (below signal cards, above last-updated row). (SharePrompt is owned by plan 02-06 and rendered in report.tsx — do not render it here.) In `mobile/components/SettingsModal.tsx`: extend the Apariencia segmented control from 3 to 4 options using the 2-row layout (Row1: Seguir sistema | Claro; Row2: Oscuro | AMOLED). The AMOLED option calls setOverride('amoled'); active option = accent fill + ink 700; "AMOLED" label untranslated. Preserve existing option behavior.
  </action>
  <verify>
    <automated>cd mobile && (npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "tabs\)/index|SettingsModal" || echo "no type errors in index/settings") && node -e "const i=require('fs').readFileSync('app/(tabs)/index.tsx','utf8'); ['shareToWhatsApp','useBattery','ContactsCard','BatteryBanner','1_800_000','600_000'].forEach(t=>{if(i.indexOf(t)<0)throw new Error('index missing '+t)}); const s=require('fs').readFileSync('components/SettingsModal.tsx','utf8'); if(s.indexOf('amoled')<0)throw new Error('settings missing amoled'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - index.tsx hero has a share button (44dp target) calling shareToWhatsApp(composeShareText(...))
    - index.tsx mounts useBattery and passes 1_800_000 when saving / 600_000 otherwise to useStatus
    - BatteryBanner shown when isBatterySaving && !bannerDismissed and takes precedence over StaleBanner; Animated.loop .stop() when saving
    - ContactsCard rendered at bottom of ScrollView with the active regionKey
    - SettingsModal Apariencia has a 4th "AMOLED" option calling setOverride('amoled')
    - `npx tsc --noEmit` reports no errors in index.tsx or SettingsModal.tsx
  </acceptance_criteria>
  <done>Zone tab gains share, contacts, prompt, battery banner + adaptive polling; settings gains AMOLED.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device sharing, contacts, AMOLED, and battery verification</name>
  <what-built>
    Zone-tab share button (WhatsApp), Números útiles contacts card,
    AMOLED settings option, and the low-battery banner + 30-min adaptive refresh.
  </what-built>
  <action>Build and run the app per your normal flow (you run it — the agent does not start a server), then walk the verification steps below.</action>
  <how-to-verify>
    1. Open the Zone tab with a selected zone. Tap the hero share button → WhatsApp opens with pre-formatted Spanish status text (status + duration; ETA line only if status.json has one). Without WhatsApp installed, the system share sheet opens instead.
    2. Scroll to "Números útiles" → tap a contact with a real number → the dialer opens with tel:. Unverified entries show "Por verificar".
    3. Settings (gear) → Apariencia → tap "AMOLED" → backgrounds turn pure black (#000000) live, no restart; reopen the app and confirm it persists.
    4. Battery (real device or simulator battery override) below 20% → "Modo ahorro activo" banner appears, shimmer animations stop; tap the banner to dismiss for the session (refresh stays at 30min). Confirm the banner takes precedence over the staleness banner.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (share text, dialer, AMOLED, battery banner).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → WhatsApp / dialer | share text + tel: number leave via OS handlers |
| battery sensor → screen | level read transiently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Information Disclosure | share text / tel rows | accept | Only public zone status + published emergency numbers; no user identity |
| T-02-17 | Tampering | unverified contact numbers | mitigate | verified:false entries flagged "Por verificar"; empty numbers non-tappable |
| T-02-18 | Denial of Service | battery drain from polling | mitigate | useStatus interval 600k→1.8M under 20%; Animated.loop stopped (BATT-02) |
</threat_model>

<verification>
- `cd mobile && npx tsc --noEmit` clean for the 3 components + index.tsx + SettingsModal.tsx
- Share, contacts, AMOLED, and battery banner verified on device (human-verify checkpoint)
</verification>

<success_criteria>
A user can share zone status to WhatsApp, call emergency contacts, switch to AMOLED true-black, and the app conserves battery below 20%. SHAR-01, BATT-01/02/03 satisfied.
</success_criteria>

<output>
Create `.planning/phases/02-reporting-sharing-quick-wins/02-07-SUMMARY.md` when done
</output>
