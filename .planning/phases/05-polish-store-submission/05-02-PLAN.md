---
phase: 05-polish-store-submission
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - mobile/components/HistoryStrip.tsx
  - mobile/components/ForecastCurve.tsx
  - mobile/app/(tabs)/history.tsx
  - mobile/__tests__/components/HistoryStrip.test.tsx
  - mobile/__tests__/components/ForecastCurve.test.tsx
autonomous: true
requirements:
  - STAT-04

must_haves:
  truths:
    - "User sees a 30-day outage strip chart in the History tab with one bar per day"
    - "User sees a 48-hour risk forecast curve below the strip chart"
    - "User sees stats cards: total hours this month, average duration, outage count"
    - "User sees an estimated return time derived from pattern.typical_duration_h when history data is present"
    - "When history is loading or null, user sees 'Historia disponible próximamente' message (not a crash)"
    - "All text is Spanish-first (matches app i18n convention)"
  artifacts:
    - path: "mobile/components/HistoryStrip.tsx"
      provides: "SVG 30-day outage bar chart using react-native-svg"
      exports: ["HistoryStrip"]
    - path: "mobile/components/ForecastCurve.tsx"
      provides: "SVG 48h risk curve using react-native-svg"
      exports: ["ForecastCurve"]
    - path: "mobile/app/(tabs)/history.tsx"
      provides: "History tab screen — replaces PlaceholderTab"
      contains: "useHistory"
    - path: "mobile/__tests__/components/HistoryStrip.test.tsx"
      provides: "Render test for empty days array"
    - path: "mobile/__tests__/components/ForecastCurve.test.tsx"
      provides: "Render test for empty forecast_48h array"
  key_links:
    - from: "mobile/app/(tabs)/history.tsx"
      to: "mobile/lib/history.ts"
      via: "useHistory hook"
      pattern: "useHistory"
    - from: "mobile/components/HistoryStrip.tsx"
      to: "react-native-svg"
      via: "Svg, Rect, Line, Text as SvgText"
      pattern: "from 'react-native-svg'"
    - from: "mobile/components/ForecastCurve.tsx"
      to: "react-native-svg"
      via: "Svg, Path, Line"
      pattern: "from 'react-native-svg'"
---

<objective>
Port HistoryStrip and ForecastCurve from web SVG to react-native-svg, build the real History tab
screen (replacing PlaceholderTab), and add render tests for both chart components.

Purpose: Delivers the visible, user-facing half of STAT-04 — outage history + return time estimate.
Output: Two SVG chart components, a working History tab screen, two render test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-polish-store-submission/05-RESEARCH.md
@.planning/phases/05-polish-store-submission/05-01-SUMMARY.md
</context>

<interfaces>
<!-- Types from Plan 01 output — executor uses these directly. -->

From mobile/lib/history.ts (created in Plan 01):
  useHistory(regionKey: string | null): UseQueryResult<RegionHistory | null, Error>
  RegionHistory.days: HistoryDay[]           — 30 entries for strip chart
  RegionHistory.forecast_48h: ForecastPoint[] — 48 * 2 = 96 half-hour points for curve
  RegionHistory.stats_30d: HistoryStats       — total_hours, count, avg_duration_h, longest_h
  RegionHistory.pattern: DetectedPattern      — typical_duration_h used for return time estimate
  HistoryDay.outages: OutageBlock[]           — each has start_hour (0-23) and duration_h
  ForecastPoint.risk: number                  — 0.0–1.0, used for curve height
  ForecastPoint.hour: number                  — x-axis label

Web source files to port (read these, then adapt):
  app/components/primitives/HistoryStrip.tsx  — source for HistoryStrip SVG logic
  app/components/primitives/ForecastCurve.tsx — source for ForecastCurve SVG logic
  app/components/mobile/ScreenHistory.tsx     — source for screen layout pattern

react-native-svg import pattern (from RESEARCH.md — alias Text to avoid RN collision):
  import Svg, { Rect, Line, Path, Text as SvgText } from 'react-native-svg';

SVG sizing rule (RESEARCH.md pitfall 3): never use width="100%" — compute pixel width:
  import { Dimensions } from 'react-native';
  const CHART_W = Math.floor(Dimensions.get('window').width - 32);  // 16px padding each side

useHistory region key source (from AppContext or MMKV storage):
  The existing history.tsx imports PlaceholderTab — executor reads mobile/contexts/AppContext.tsx
  or mobile/lib/storage.ts to find the stored region key (same key used in Zone tab).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port HistoryStrip and ForecastCurve to react-native-svg with render tests</name>
  <files>
    mobile/components/HistoryStrip.tsx,
    mobile/components/ForecastCurve.tsx,
    mobile/__tests__/components/HistoryStrip.test.tsx,
    mobile/__tests__/components/ForecastCurve.test.tsx
  </files>
  <read_first>
    app/components/primitives/HistoryStrip.tsx — read fully; copy coordinate logic verbatim; only change imports
    app/components/primitives/ForecastCurve.tsx — read fully; copy coordinate logic verbatim; only change imports
    mobile/jest.setup.js — confirm react-native-svg mock is present (added in Plan 01 Task 1)
    .planning/phases/05-polish-store-submission/05-RESEARCH.md — Code Examples section (HistoryStrip port), Pitfall 1 (Text alias), Pitfall 3 (pixel width)
  </read_first>
  <behavior>
    HistoryStrip render tests:
    - Renders without crashing when days=[] (empty array)
    - Renders without crashing when days has 30 entries with zero outages
    - Does not crash when an OutageBlock has duration_h=0

    ForecastCurve render tests:
    - Renders without crashing when forecast_48h=[] (empty array)
    - Renders without crashing when all risk values are 0
    - Renders without crashing when all risk values are 1
  </behavior>
  <action>
    Create mobile/components/HistoryStrip.tsx by porting app/components/primitives/HistoryStrip.tsx:
    - Replace `import React from 'react'` SVG usage with `import Svg, { Rect, Line, Text as SvgText } from 'react-native-svg'`
    - Replace any `width="100%"` with numeric prop: accept `width?: number` prop defaulting to Math.floor(Dimensions.get('window').width - 32)
    - Keep CHART_H constant (96 or matching web source)
    - All coordinate math from the web version is unchanged — it's pure TypeScript
    - Accept props: `days: HistoryDay[]` (required)

    Create mobile/components/ForecastCurve.tsx by porting app/components/primitives/ForecastCurve.tsx:
    - Same import swap: Svg, Path, Line from react-native-svg
    - Same numeric width pattern
    - Accept props: `forecast_48h: ForecastPoint[]` (required)

    Create both test files using react-test-renderer (same pattern as other component tests in mobile/__tests__/components/). Import the component, render with empty/minimal props, assert no crash (use expect(rendered).toBeTruthy() or snapshot). The SVG mock in jest.setup.js handles native module absence.
  </action>
  <verify>
    <automated>npm test --prefix mobile -- --testPathPattern="HistoryStrip|ForecastCurve" --passWithNoTests</automated>
  </verify>
  <done>
    Both test files pass. HistoryStrip and ForecastCurve render with empty arrays without crashing.
    No 'react-native-svg Text' type errors (Text is aliased as SvgText).
    No width="100%" strings in either component file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace history.tsx PlaceholderTab with real History screen</name>
  <files>mobile/app/(tabs)/history.tsx</files>
  <read_first>
    mobile/app/(tabs)/history.tsx — current file (5 lines, returns PlaceholderTab — replace entirely)
    app/components/mobile/ScreenHistory.tsx — web screen layout source (layout pattern only; adapt RN imports)
    mobile/app/(tabs)/zone.tsx — read for how it reads the selected region from context/storage (mirror that pattern)
    mobile/contexts/AppContext.tsx — find selectedRegion or regionKey accessor used in other tabs
    mobile/components/HistoryStrip.tsx — just created in Task 1 (import from there)
    mobile/components/ForecastCurve.tsx — just created in Task 1
    mobile/lib/history.ts — useHistory hook (from Plan 01)
  </read_first>
  <action>
    Replace mobile/app/(tabs)/history.tsx entirely. New screen:

    1. Read the selected region key from AppContext (same accessor used in zone.tsx / food.tsx).

    2. Call `useHistory(regionKey)` from mobile/lib/history.ts. Destructure `{ data: history, isLoading }`.

    3. If isLoading: render a centered ActivityIndicator.

    4. If !history: render a centered Text with Spanish copy "Historia disponible próximamente" and subtext "Los datos se actualizan semanalmente." Use the theme's text color from useTheme() (same hook used in other tabs).

    5. When history is present, render a ScrollView with:
       a. Section header: region display_name + "Últimos 30 días"
       b. HistoryStrip component passing days={history.days}
       c. Stats row: three small cards — "Este mes" (stats_30d.total_hours + "h sin luz"), "Promedio" (stats_30d.avg_duration_h.toFixed(1) + "h"), "Cortes" (stats_30d.count.toString())
       d. If history.pattern.detected: show a "Patrón detectado" card with history.pattern.description and "Duración típica: X h" from history.pattern.typical_duration_h
       e. Section header "Pronóstico 48h"
       f. ForecastCurve component passing forecast_48h={history.forecast_48h}
       g. If region entry from useStatus has outage?.estimated_restoration: show "Restauración estimada: {date}" (optional — skip if useStatus data not available)

    TypeScript strict mode: no `any` types. All optional fields guarded with `?.` or null checks.
    Functional component only. No class components.
  </action>
  <verify>
    <automated>npm test --prefix mobile -- --passWithNoTests</automated>
  </verify>
  <done>
    Full test suite passes (no regressions). history.tsx no longer imports PlaceholderTab.
    history.tsx imports useHistory, HistoryStrip, ForecastCurve.
    grep confirms: grep -c "PlaceholderTab" mobile/app/(tabs)/history.tsx outputs 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN JSON → React render | history.days and forecast_48h rendered into SVG coordinates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | HistoryStrip coordinate math | accept | All data from public read-only CDN; null/empty array guards prevent crash; no user-controlled input |
| T-05-04 | Denial of Service | ForecastCurve with 96-point path | accept | Path computation is O(n) on 96 fixed points; no performance risk |
| T-05-SC | Tampering | react-native-svg native module | mitigate | Verified package legitimacy in 05-01 (npm, MIT, software-mansion); jest mock prevents test-time native calls |
</threat_model>

<verification>
npm test --prefix mobile -- --testPathPattern="HistoryStrip|ForecastCurve"
npm test --prefix mobile
grep -c "PlaceholderTab" mobile/app/(tabs)/history.tsx   # must be 0
grep -c "useHistory" mobile/app/(tabs)/history.tsx       # must be >= 1
grep -c "width=\"100%\"" mobile/components/HistoryStrip.tsx    # must be 0
grep -c "width=\"100%\"" mobile/components/ForecastCurve.tsx   # must be 0
</verification>

<success_criteria>
- HistoryStrip renders with empty days[] without crashing
- ForecastCurve renders with empty forecast_48h[] without crashing
- history.tsx imports useHistory, HistoryStrip, ForecastCurve — no PlaceholderTab
- History tab shows stats (total_hours, count, avg_duration_h) from stats_30d when data available
- History tab shows graceful null-state "Historia disponible próximamente" when data is null
- Full test suite npm test --prefix mobile exits 0
</success_criteria>

<output>
Create `.planning/phases/05-polish-store-submission/05-02-SUMMARY.md` when done.
</output>
