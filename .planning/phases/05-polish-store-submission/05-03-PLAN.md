---
phase: 05-polish-store-submission
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - mobile/eas.json
  - mobile/app.json
autonomous: false
requirements:
  - PLAT-04
  - PLAT-05

must_haves:
  truths:
    - "eas.json contains a submit.production.android section with serviceAccountKeyPath and track fields"
    - "eas.json contains a submit.production.ios section with appleId, ascAppId placeholder, and appleTeamId placeholder"
    - "app.json ios section has privacy description strings for any permissions the app requests"
    - "A production Android AAB is built by EAS Build cloud and downloadable from EAS dashboard"
    - "The AAB is manually uploaded to Google Play Console internal track by the developer"
    - "EAS Submit can push subsequent Android builds to Play Console once the first manual upload is done"
    - "iOS submission is tracked as blocked on Apple Developer Program enrollment"
  artifacts:
    - path: "mobile/eas.json"
      provides: "submit.production profiles for android and ios"
      contains: "submit"
    - path: "mobile/app.json"
      provides: "production-ready metadata: version, versionCode, buildNumber, bundleIdentifier"
      contains: "versionCode"
  key_links:
    - from: "eas.json submit.production.android"
      to: "Google Play Console"
      via: "EAS Submit CLI after first manual AAB upload"
      pattern: "serviceAccountKeyPath"
    - from: "eas.json submit.production.ios"
      to: "App Store Connect"
      via: "EAS Submit CLI (blocked until Apple Dev Program active)"
      pattern: "ascAppId"
---

<objective>
Add eas.json submit profiles for Android and iOS, verify app.json production metadata, trigger
the EAS production Android build, then gate on two human checkpoints: first manual AAB upload to
Play Console, and Apple Developer Program enrollment for iOS.

Purpose: Delivers PLAT-04 (Google Play) and PLAT-05 (App Store) — Android track is independent
of iOS and can ship first. iOS is formally gated pending $99/yr Apple enrollment.
Output: Updated eas.json with submit profiles, production Android AAB, human checklist gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-polish-store-submission/05-RESEARCH.md
@.planning/phases/05-polish-store-submission/05-02-SUMMARY.md
</context>

<interfaces>
<!-- Current eas.json structure — executor reads this before editing. -->

Current mobile/eas.json:
  cli: { version: ">= 19.0.0", appVersionSource: "remote" }
  build:
    development: { developmentClient: true, distribution: "internal", environment: "development" }
    preview:     { distribution: "internal", channel: "preview", environment: "preview" }
    production:  { channel: "production", environment: "production" }
  (no "submit" key yet — must be added)

Current mobile/app.json relevant fields:
  version: "1.0.0"
  android.package: "app.cocuyo.mobile"
  android.versionCode: 1
  ios.bundleIdentifier: "app.cocuyo.mobile"
  ios.buildNumber: "1"
  ios.infoPlist.ITSAppUsesNonExemptEncryption: false  ← already set, do not change

EAS Submit Android docs: docs.expo.dev/submit/android/
  - track: "internal" for first submission (graduate to production manually in Play Console)
  - serviceAccountKeyPath: path to Google Service Account JSON (stored in mobile/credentials/, gitignored)
  - First upload MUST be manual (Google Play API cannot create a new app listing)

EAS Submit iOS docs: docs.expo.dev/submit/ios/
  - appleId: "leonardoduam@gmail.com"
  - ascAppId: placeholder "<APP_STORE_CONNECT_APP_ID>" (set after human creates App Store Connect record)
  - appleTeamId: placeholder "<APPLE_TEAM_ID>" (set after human enrolls in Apple Developer Program)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add eas.json submit profiles and verify app.json production metadata</name>
  <files>mobile/eas.json, mobile/app.json</files>
  <read_first>
    mobile/eas.json — read current content fully before editing (add submit key alongside existing build key)
    mobile/app.json — read current content fully; verify version/versionCode/buildNumber/bundleIdentifier
    .planning/phases/05-polish-store-submission/05-RESEARCH.md — Pattern 3 (store submission eas.json), Pitfall 4 (first manual upload required), Pitfall 5 (Apple Dev Program blocker)
  </read_first>
  <action>
    Edit mobile/eas.json: add a top-level "submit" key alongside the existing "build" key:

      submit.production.android:
        serviceAccountKeyPath: "./credentials/google-service-account.json"
        track: "internal"

      submit.production.ios:
        appleId: "leonardoduam@gmail.com"
        ascAppId: "<APP_STORE_CONNECT_APP_ID>"
        appleTeamId: "<APPLE_TEAM_ID>"

    The ios placeholders remain as literal strings — they are filled by the human in the iOS gate checkpoint below.

    Verify mobile/app.json production metadata (do NOT change values that are already correct; only fix if wrong):
      - version must be "1.0.0"
      - android.versionCode must be 1 (integer)
      - ios.buildNumber must be "1" (string)
      - android.package must be "app.cocuyo.mobile"
      - ios.bundleIdentifier must be "app.cocuyo.mobile"
      - ios.infoPlist.ITSAppUsesNonExemptEncryption must remain false

    Do NOT add the Google Service Account JSON file itself — that is a human-supplied credential.
    Do NOT add any API keys or secrets to any committed file.

    Verify the eas.json edit is valid JSON: run `node -e "require('./mobile/eas.json')"` after editing.
  </action>
  <verify>
    <automated>node -e "const e = require('./mobile/eas.json'); const a = e.submit.production.android; const i = e.submit.production.ios; if (!a.serviceAccountKeyPath || !a.track) process.exit(1); if (!i.appleId) process.exit(1); console.log('eas.json submit profiles valid')"</automated>
  </verify>
  <done>
    eas.json parses as valid JSON. submit.production.android has serviceAccountKeyPath and track="internal".
    submit.production.ios has appleId="leonardoduam@gmail.com". No secrets committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Gate A: Android — Google Play Developer account + EAS production build + first manual AAB upload</name>
  <read_first>
    .planning/phases/05-polish-store-submission/05-RESEARCH.md — STORE SUBMISSION section, Google Play gate table (G-P1 through G-P9), Pitfall 4 (first manual upload required)
  </read_first>
  <what-built>
    eas.json now has submit.production.android with track="internal" and serviceAccountKeyPath pointing to
    mobile/credentials/google-service-account.json. The production build profile was already present.
  </what-built>
  <how-to-verify>
    Complete these steps in order. Each has an observable outcome.

    STEP 1 — Create Google Play Developer account ($25 one-time):
      Visit https://play.google.com/apps/publish and enroll.
      Observable: Developer Console dashboard loads with your account.

    STEP 2 — Create app in Play Console:
      Click "Create app". Package name must be "app.cocuyo.mobile".
      Observable: App page exists at play.google.com/apps/publish with package "app.cocuyo.mobile".

    STEP 3 — Generate Google Service Account JSON for EAS Submit:
      Follow docs.expo.dev/submit/android/ (section "Creating a Google Service Account").
      Download the JSON key file. Place it at mobile/credentials/google-service-account.json.
      (credentials/ is in .gitignore — confirm before placing file.)
      Observable: mobile/credentials/google-service-account.json exists on disk.

    STEP 4 — Build production Android AAB via EAS:
      Run from repo root: `cd mobile && eas build --platform android --profile production`
      This uploads to EAS cloud. Takes 5-15 minutes. EAS CLI prints a dashboard URL.
      Observable: Build status "Finished" at expo.dev/accounts/kralgor/projects/cocuyo/builds.
      Download the .aab file from the EAS dashboard.

    STEP 5 — First manual upload to Play Console:
      In Play Console: Internal testing > Create new release > Upload the .aab file.
      Observable: Release shows in Play Console internal testing with the uploaded AAB.

    STEP 6 — Complete Play Console listing (can be done in parallel with Step 4-5):
      Required fields: short description (80 chars max), full description (Spanish primary),
      at least 2 screenshots, feature graphic (1024×500), content rating questionnaire,
      data safety form (declare: no user data collected, anonymous reports only),
      privacy policy URL (must be a live public page — e.g., a GitHub Gist or GitHub Pages URL).
      Observable: All required fields show green checkmarks in Play Console dashboard.

    STEP 7 — After first manual upload, test EAS Submit CLI:
      Run: `cd mobile && eas submit --platform android --profile production --latest`
      Observable: EAS Submit CLI prints "Submission successful" or similar success message.
  </how-to-verify>
  <acceptance_criteria>
    - Google Play Developer account exists and Play Console loads
    - App "app.cocuyo.mobile" exists in Play Console
    - mobile/credentials/google-service-account.json exists on disk (not committed to git)
    - Production AAB built successfully by EAS (visible in EAS dashboard)
    - AAB uploaded to Play Console internal testing track (visible in Play Console release page)
    - Play Console listing has all required fields complete (no red warning indicators)
    - `eas submit --platform android --profile production --latest` exits without "App not found" error
  </acceptance_criteria>
  <resume-signal>Type "android-live" when the AAB is visible in Play Console internal testing, OR describe any gate that is blocked</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Gate B: iOS — Apple Developer Program enrollment + App Store Connect record + iOS production build</name>
  <read_first>
    .planning/phases/05-polish-store-submission/05-RESEARCH.md — Apple App Store gate table (A-P1 through A-P7), Pitfall 5 (Apple Dev Program active blocker from Phase 1)
  </read_first>
  <what-built>
    eas.json submit.production.ios has appleId set. ascAppId and appleTeamId are placeholders
    that must be filled after human completes App Store Connect enrollment.
  </what-built>
  <how-to-verify>
    NOTE: This gate is INDEPENDENT of Gate A. Android can ship first. iOS proceeds only after
    Apple Developer Program is active (outstanding blocker from Phase 1, STATE.md).

    STEP 1 — Enroll in Apple Developer Program ($99/yr):
      Visit https://developer.apple.com/programs/ and complete enrollment.
      Observable: developer.apple.com/account shows active membership with Team ID.
      Note your APPLE_TEAM_ID (10-character string like "ABCD123456").

    STEP 2 — Create App ID and App Store Connect record:
      In App Store Connect (appstoreconnect.apple.com): My Apps > "+" > New App.
      Bundle ID: "app.cocuyo.mobile". Platform: iOS.
      Observable: App record exists in App Store Connect with numeric App ID (ASC App ID).
      Note your ASC_APP_ID (numeric, e.g. "1234567890").

    STEP 3 — Update eas.json ios placeholders:
      Edit mobile/eas.json submit.production.ios:
        ascAppId: "<replace with your numeric ASC App ID>"
        appleTeamId: "<replace with your 10-char Team ID>"

    STEP 4 — Build production iOS IPA via EAS:
      Run: `cd mobile && eas build --platform ios --profile production`
      EAS handles managed credentials (no local Xcode required — works on Windows/Linux).
      Observable: Build status "Finished" at expo.dev/accounts/kralgor/projects/cocuyo/builds.

    STEP 5 — Complete App Store Connect listing:
      Required: screenshots for 6.7" (iPhone 16 Pro Max) and 6.1" (iPhone 16) sizes,
      app description (Spanish primary), keywords, subtitle, privacy policy URL,
      privacy questionnaire (declare: no tracking, anonymous reports only, no data collection).
      Observable: App Store Connect listing shows no red validation errors.

    STEP 6 — Submit to App Review via EAS Submit:
      Run: `cd mobile && eas submit --platform ios --profile production --latest`
      Observable: App appears in App Store Connect "In Review" status (review takes 1-3 days typical).
  </how-to-verify>
  <acceptance_criteria>
    - Apple Developer Program active (visible at developer.apple.com/account)
    - App record exists in App Store Connect with a numeric ascAppId
    - eas.json submit.production.ios has ascAppId and appleTeamId replaced (no placeholders)
    - Production iOS IPA built by EAS (visible in EAS dashboard)
    - App Store Connect listing has no validation errors
    - App is in "In Review" or "Ready for Sale" status in App Store Connect
  </acceptance_criteria>
  <resume-signal>Type "ios-review" when the iOS build is submitted to App Review, OR "ios-deferred" if Apple Developer Program enrollment is being skipped for now</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → EAS Cloud | Build artifacts and credentials transmitted over HTTPS |
| EAS Cloud → Google Play API | AAB submitted via service account key (not committed to git) |
| EAS Cloud → Apple / App Store Connect | IPA submitted via Apple managed credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | Google Service Account JSON | mitigate | File stored in mobile/credentials/ (gitignored); never committed; EAS secrets used for CI |
| T-05-06 | Information Disclosure | Apple Developer credentials | mitigate | EAS Managed Credentials handles Apple cert/profile storage; no local keychain export committed |
| T-05-07 | Information Disclosure | supabaseAnonKey in app.json | accept | Anon key is public by design (ADR-007); service_role key is never present |
| T-05-SC | Tampering | eas-cli npm install | mitigate | eas-cli is official Expo toolchain; already installed globally; no new install in this plan |
</threat_model>

<verification>
node -e "const e = require('./mobile/eas.json'); console.log(JSON.stringify(e.submit, null, 2))"
# Confirm: submit.production.android.track = "internal"
# Confirm: submit.production.ios.appleId = "leonardoduam@gmail.com"

node -e "const a = require('./mobile/app.json'); const e = a.expo; console.log(e.version, e.android.versionCode, e.ios.buildNumber, e.ios.bundleIdentifier)"
# Expected output: 1.0.0 1 1 app.cocuyo.mobile

git status mobile/credentials/
# Must NOT show google-service-account.json as tracked
</verification>

<success_criteria>
- eas.json valid JSON with submit.production.android (track=internal) and submit.production.ios (appleId set)
- app.json production metadata correct (no placeholders in code-controlled fields)
- Production Android AAB built and uploaded to Play Console internal testing (PLAT-04 satisfied)
- App listed in Play Console with all required fields complete
- iOS build submitted to App Review (PLAT-05 satisfied) OR formally deferred with "ios-deferred" signal
- No secrets committed to git (google-service-account.json remains in credentials/, gitignored)
</success_criteria>

<output>
Create `.planning/phases/05-polish-store-submission/05-03-SUMMARY.md` when done.
</output>
