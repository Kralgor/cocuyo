---
phase: 01-foundation-offline-core
plan: 05
type: execute
wave: 6
depends_on: ["01-03", "01-04"]
files_modified:
  - mobile/eas.json
autonomous: false
requirements: [PLAT-01, PLAT-02, PLAT-03, TRST-01, TRST-02, STAT-01, STAT-02, STAT-03]

must_haves:
  truths:
    - "An EAS development build is produced for Android (API 24+) and iOS (15+) (PLAT-01, PLAT-02)"
    - "The app launches on device/emulator and the full first-launch flow works (trust → zone picker → tabs)"
    - "Offline cached status + staleness banner verified by toggling airplane mode (STAT-03)"
    - "An EAS Update (OTA) is published and received by the running dev build (PLAT-03)"
    - "Settings privacy section opens and the GitHub link launches the browser (TRST-02)"
  artifacts:
    - path: "mobile/eas.json"
      provides: "Verified EAS build + update profiles"
      contains: "development"
  key_links:
    - from: "eas build"
      to: "device/emulator"
      via: "EAS cloud build artifact install"
      pattern: "development"
---

<objective>
Produce EAS development builds and run the manual-only platform and UI-flow verifications from VALIDATION.md that cannot be unit-tested: native launch on Android + iOS, the offline/staleness behavior under real network toggling, OTA update delivery, and the trust/settings UI flow.

Purpose: Close PLAT-01/02/03 (which are native-build and OTA verifications) and confirm the end-to-end Phase 1 success criteria on a real device. This is the phase's verification gate.
Output: Confirmed dev builds on both platforms, a delivered OTA update, and a human sign-off on the trust/offline/settings flows.

Note: This machine is WSL (no Xcode, no local Android SDK per RESEARCH.md Environment Availability). iOS builds MUST use EAS cloud build; Android may use EAS cloud build or a connected device. Claude orchestrates the EAS CLI; the human installs artifacts and verifies on-device.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-offline-core/01-RESEARCH.md
@.planning/phases/01-foundation-offline-core/01-VALIDATION.md
@.planning/phases/01-foundation-offline-core/01-01a-SUMMARY.md
@.planning/phases/01-foundation-offline-core/01-03-SUMMARY.md
@.planning/phases/01-foundation-offline-core/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger EAS development builds (Android + iOS)</name>
  <files>mobile/eas.json</files>
  <read_first>
    - .planning/phases/01-foundation-offline-core/01-RESEARCH.md (Environment Availability — WSL/no Xcode → EAS cloud; eas.json profiles)
    - .planning/phases/01-foundation-offline-core/01-VALIDATION.md (Manual-Only Verifications — build commands)
    - mobile/eas.json (existing profiles from Plan 01)
    - mobile/app.json (minSdkVersion 24, iOS deploymentTarget 15.0)
  </read_first>
  <action>
    Confirm eas.json has a development profile (developmentClient true, distribution internal). Run a full typecheck and test pass first (`cd mobile && npx tsc --noEmit && npx jest`) — do not build red code. Then trigger EAS cloud development builds: `cd mobile && eas build --profile development --platform android` and `eas build --profile development --platform ios`. These require Leo's expo.dev auth — if `eas whoami` fails, surface an auth checkpoint (Leo runs `eas login`). Capture the build artifact URLs from the EAS output for the human-verify checkpoint. Run `npx expo-doctor` and record any warning about the iOS 15.0 deploymentTarget (RESEARCH.md Assumption A1 / Open Question 1) — if expo-doctor flags iOS 15 as unachievable, note it for the checkpoint so Leo can decide whether to accept 16.4 and update PLAT-02. Do NOT run a local `expo run:*` build (no native toolchain on WSL).
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit && npx jest</automated>
  </verify>
  <acceptance_criteria>
    - `cd mobile && npx tsc --noEmit` exits 0 and `cd mobile && npx jest` exits 0 before any build is triggered
    - EAS development builds triggered for both android and ios (build URLs captured in the SUMMARY)
    - expo-doctor run; any iOS 15.0 deploymentTarget warning recorded for the checkpoint
    - mobile/eas.json development profile has developmentClient true and distribution internal
  </acceptance_criteria>
  <done>EAS cloud development builds are running/complete for Android and iOS; artifact URLs captured; expo-doctor findings recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device verification of Phase 1 success criteria</name>
  <files>mobile/eas.json</files>
  <action>
    Pause for human on-device verification (blocking gate — do not auto-advance). Present the EAS build artifact URLs from Task 1 and walk the human through the verification steps in &lt;how-to-verify&gt;. If `eas update` auth or build artifacts are unavailable, surface an auth checkpoint before proceeding. Record the human's pass/fail per step and the iOS 15 vs 16.4 decision in the SUMMARY.
  </action>
  <what-built>
    EAS development builds for Android (API 24+) and iOS (15+). The app implements: trust onboarding (first launch), zone picker (state-grouped, searchable, status dots), 5-tab bar with 4 placeholders, zone detail (hero status + duration + signal cards), non-dismissible staleness banner, offline cache via MMKV + React Query persister, settings modal (privacy + GitHub + theme + zone), and EAS OTA updates.
  </what-built>
  <how-to-verify>
    1. Install the Android dev build (from the EAS artifact URL) on an API 24 emulator or device; install the iOS dev build on an iOS 15 simulator/device (or TestFlight internal). Confirm the app launches without a white-screen flash (PLAT-01, PLAT-02).
    2. First launch: confirm the trust onboarding screen appears with 4 trust points, the GitHub link, and "Comenzar". Tap the GitHub link — browser opens the repo (TRST-01).
    3. Tap "Comenzar" → zone picker appears. Search "mara" → only Maracaibo and Maracay show. Pick a zone → tabs appear on "Mi Zona" (D-10, D-11).
    4. Zone detail: confirm the hero block shows the correct color + Spanish status label, and (if the zone has an active outage) a "Hace Xh Ym" duration. Confirm 3 signal cards (Internet/Reportes/Satélite) with bars (STAT-01, STAT-02).
    5. Kill and relaunch the app — the trust screen must NOT reappear; it opens directly to the saved zone (D-03, D-08).
    6. Enable airplane mode and relaunch — cached status displays and the amber non-dismissible staleness banner appears once cache is >15 min old (STAT-03, D-13).
    7. Toggle device dark/light mode — the app theme follows the system preference (D-06). Open Settings (gear) → confirm the privacy/open-source section and that the GitHub link opens the browser (TRST-02). Try the Apariencia segmented control overrides.
    8. OTA: Claude runs `eas update --branch preview` (or production). Reload the dev build and confirm the update is received (PLAT-03).
  </how-to-verify>
  <verify>
    <human-check>All 8 steps pass on both Android and iOS; OTA update received.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe any failures (which step, which platform). If expo-doctor flagged iOS 15, decide: keep 15.0 or accept 16.4 and update PLAT-02.</resume-signal>
  <done>Human confirms all 8 Phase 1 success criteria on-device for Android + iOS; OTA delivered; iOS deployment-target decision recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EAS cloud build → device artifact | build pipeline produces installable binaries |
| EAS Update channel → running app | OTA JS bundle delivered to devices |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | EAS Update OTA bundle | mitigate | expo-updates uses EAS code signing on the update channel; runtimeVersion appVersion policy prevents incompatible bundles loading. |
| T-01-11 | Spoofing | EAS build auth | mitigate | Builds run under Leo's authenticated expo.dev account; auth gate surfaced as checkpoint if `eas whoami` fails. |
| T-01-SC | Tampering | npm package installs | mitigate | No new packages installed; build consumes Plan 01's audited dependency set. |
</threat_model>

<verification>
- `cd mobile && npx tsc --noEmit` exits 0 and `cd mobile && npx jest` exits 0 before build
- EAS development build artifacts produced for both platforms (URLs in SUMMARY)
- Human sign-off recorded on all 8 verification steps
- OTA update received by the running dev build (PLAT-03)
</verification>

<success_criteria>
- App launches on Android (API 24+) and iOS (15+) dev builds (PLAT-01, PLAT-02)
- Trust onboarding shows once; settings privacy + GitHub link work (TRST-01, TRST-02)
- Status, duration, and offline staleness banner verified on-device (STAT-01/02/03)
- OTA update delivered (PLAT-03)
- All five ROADMAP Phase 1 success criteria confirmed TRUE
</success_criteria>

<output>
Create `.planning/phases/01-foundation-offline-core/01-05-SUMMARY.md` when done
</output>
