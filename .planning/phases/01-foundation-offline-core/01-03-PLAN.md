---
phase: 01-foundation-offline-core
plan: 03
type: execute
wave: 4
depends_on: ["01-02"]
files_modified:
  - mobile/app/onboarding.tsx
  - mobile/app/zone-picker.tsx
  - mobile/components/ZonePicker.tsx
autonomous: true
requirements: [TRST-01, STAT-01]

must_haves:
  truths:
    - "First launch shows a single full-screen trust onboarding screen with 4 trust points, GitHub link, and Comenzar CTA (TRST-01, D-07)"
    - "Tapping Comenzar sets hasSeenOnboarding in MMKV; Stack.Protected then shows the zone picker (D-08, D-10)"
    - "GitHub link opens the repo in the browser via Linking.openURL"
    - "Zone picker lists 17 zones grouped by state with a search bar and per-zone status dots (D-11)"
    - "Selecting a zone writes selectedZone to MMKV; Stack.Protected then shows the tabs"
    - "Search filters zones case-insensitively and removes empty sections; empty result shows the no-results copy"
  artifacts:
    - path: "mobile/app/onboarding.tsx"
      provides: "Trust onboarding screen"
      contains: "hasSeenOnboarding"
    - path: "mobile/components/ZonePicker.tsx"
      provides: "SectionList zone picker with search + status dots"
      contains: "SectionList"
    - path: "mobile/app/zone-picker.tsx"
      provides: "Zone picker route writing selectedZone"
      contains: "selectedZone"
  key_links:
    - from: "mobile/app/onboarding.tsx"
      to: "mobile/lib/storage.ts"
      via: "storage.set(hasSeenOnboarding, true) on Comenzar"
      pattern: "hasSeenOnboarding"
    - from: "mobile/components/ZonePicker.tsx"
      to: "mobile/lib/regions.ts"
      via: "ZONE_SECTIONS + REGIONS for grouped list"
      pattern: "ZONE_SECTIONS"
    - from: "mobile/components/ZonePicker.tsx"
      to: "mobile/hooks/useStatus.ts"
      via: "status dots colored from live status data"
      pattern: "useStatus"
---

<objective>
Build the first-launch flow: the trust onboarding screen and the zone picker (route + reusable ZonePicker component). These are the two Stack.Protected-gated screens that precede the main tabs.

Purpose: Deliver TRST-01 (trust onboarding) and zone selection. This plan owns the onboarding/zone-picker routes and the ZonePicker component; it consumes the shell and hooks from Plan 02. Runs in parallel with Plan 04 (no shared files).
Output: A working first-launch sequence — trust screen → Comenzar → zone picker (grouped, searchable, status dots) → tap zone → tabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-offline-core/01-RESEARCH.md
@.planning/phases/01-foundation-offline-core/01-PATTERNS.md
@.planning/phases/01-foundation-offline-core/01-UI-SPEC.md
@.planning/phases/01-foundation-offline-core/01-02-SUMMARY.md

<interfaces>
<!-- From Plans 01/02 — use directly -->
mobile/lib/storage.ts: storage, STORAGE_KEYS { hasSeenOnboarding, selectedZone }
mobile/lib/regions.ts: REGIONS (Record<string,RegionMeta>), ZONE_SECTIONS ({title,data:string[]}[]), filterSections(query)
mobile/lib/i18n.ts: tt(key, lang), Lang — keys: trust points, comenzar, search_placeholder, no_results
mobile/lib/theme.ts: statusColor(status, theme)
mobile/hooks/useTheme.ts: useTheme() → { theme }
mobile/hooks/useStatus.ts: useStatus() → { data: StatusJson|null }
expo-router: useRouter, navigation handled by Stack.Protected re-render on MMKV write
expo-linking: Linking.openURL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trust onboarding screen (TRST-01, D-07/D-08/D-10)</name>
  <files>mobile/app/onboarding.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-offline-core/01-UI-SPEC.md (Trust Onboarding screen interaction contract + Copywriting Contract trust strings + spacing/typography)
    - .planning/phases/01-foundation-offline-core/01-PATTERNS.md (onboarding.tsx MMKV write-on-complete pattern)
    - .planning/phases/01-foundation-offline-core/01-RESEARCH.md (Pattern 2 Stack.Protected — no manual router.replace)
    - mobile/lib/storage.ts, mobile/hooks/useTheme.ts, mobile/lib/i18n.ts
  </read_first>
  <action>
    Full-screen centered column, safe-area top/bottom, no scroll (content fits viewport per UI-SPEC). Layout top-to-bottom: Cocuyo logo (80dp wide, 32dp margin-bottom — use a placeholder Ionicons or Text wordmark if no asset exists, note the asset gap), subtitle tt('trust_subtitle', lang) (16sp inkDim centered), four trust-point rows each = Ionicons checkmark-circle (20dp, theme.ok) + 12dp gap + column(heading 16sp ink bold + body 13sp inkDim) using the trust point strings from UI-SPEC Copywriting Contract, 24dp separator, GitHub link Pressable (Ionicons logo-github 18dp + tt('github_link') label, accent bottom-border 1dp, no fill) → Linking.openURL('https://github.com/kralgor/cocuyo'), 16dp spacer, "Comenzar" full-width 48dp button (accent fill #E8C840, ink text — readable on yellow, 16sp bold, 8dp radius) → handleComplete sets storage.set(STORAGE_KEYS.hasSeenOnboarding, true). Do NOT call router.replace — Stack.Protected in _layout re-renders the guard automatically (Pattern 2). Detect lang from expo-localization (or a shared helper). StyleSheet.create for all layout (D-04). accessibilityLabel on the gear/links per UI-SPEC Accessibility.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit && grep -q "hasSeenOnboarding" app/onboarding.tsx && grep -q "openURL" app/onboarding.tsx</automated>
  </verify>
  <acceptance_criteria>
    - mobile/app/onboarding.tsx renders 4 trust-point rows (4 checkmark-circle Ionicons)
    - mobile/app/onboarding.tsx sets STORAGE_KEYS.hasSeenOnboarding to true on the Comenzar press
    - mobile/app/onboarding.tsx contains "Linking.openURL" pointing at the GitHub repo
    - mobile/app/onboarding.tsx does NOT call router.replace or router.push (Stack.Protected handles navigation)
    - Uses StyleSheet.create (no inline layout style objects)
    - `cd mobile && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Trust screen shows 4 trust points + GitHub link + Comenzar; Comenzar flips the MMKV flag so Stack.Protected advances to the zone picker (TRST-01, D-07/08/10).</done>
</task>

<task type="auto">
  <name>Task 2: ZonePicker component + zone-picker route (D-11, STAT-01)</name>
  <files>mobile/components/ZonePicker.tsx, mobile/app/zone-picker.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-offline-core/01-UI-SPEC.md (Zone Picker screen interaction contract: search bar, SectionList, zone row, status dot, empty state)
    - .planning/phases/01-foundation-offline-core/01-PATTERNS.md (ZonePicker.tsx pattern — filterSections, status dot color)
    - .planning/phases/01-foundation-offline-core/01-RESEARCH.md (Pattern 6 SectionList + search)
    - mobile/lib/regions.ts (REGIONS, ZONE_SECTIONS, filterSections), mobile/lib/theme.ts (statusColor), mobile/hooks/useStatus.ts, mobile/lib/storage.ts
  </read_first>
  <action>
    ZonePicker.tsx: a reusable component taking an onSelect(zoneKey) callback. Header title tt('zone_picker_title', lang) (20sp ink). Search TextInput (48dp height, 16dp horizontal margin, panel bg, 8dp radius, inkFaint placeholder tt('search_placeholder')). SectionList over filterSections(query) from regions.ts — section headers are uppercase state names (13sp inkDim, xl left padding), zone rows 48dp height panel bg with a status dot (8dp circle colored via statusColor(status, theme) using live status from useStatus().data.regions[key]?.status, inkFaint if no data) + 8dp gap + REGIONS[key].display_name (16sp ink). Row tap → onSelect(key). Empty filtered result → centered tt('no_results') with the query interpolated (16sp inkDim). accessibilityLabel "{display_name}, {statusLabel}" per UI-SPEC. zone-picker.tsx: renders <ZonePicker onSelect={key => storage.set(STORAGE_KEYS.selectedZone, key)} /> — the MMKV write triggers the Stack.Protected guard to advance to (tabs); no manual navigation. StyleSheet.create (D-04). The same ZonePicker component is reused by the Settings "Cambiar zona" action in Plan 04, so keep it presentational and callback-driven.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit && grep -q "SectionList" components/ZonePicker.tsx && grep -q "selectedZone" app/zone-picker.tsx && grep -q "statusColor" components/ZonePicker.tsx</automated>
  </verify>
  <acceptance_criteria>
    - mobile/components/ZonePicker.tsx renders a SectionList driven by filterSections + a search TextInput
    - mobile/components/ZonePicker.tsx colors status dots via statusColor() using useStatus live data, inkFaint fallback when no data
    - mobile/components/ZonePicker.tsx takes an onSelect callback (reusable by Settings in Plan 04)
    - mobile/app/zone-picker.tsx sets STORAGE_KEYS.selectedZone on selection and does NOT call router.replace/push
    - empty search renders the no_results copy with the query interpolated
    - `cd mobile && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Zone picker lists 17 state-grouped zones with search and live status dots; selecting writes selectedZone so Stack.Protected advances to the tabs (D-11, STAT-01 status data surfaced as dots).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (search box) → SectionList filter | free-text search string used only for client-side filtering |
| External link → browser | Linking.openURL to GitHub repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | search filter input | accept | Search string only filters an in-memory array via toLowerCase().includes(); no injection surface, no eval, no query to backend. |
| T-01-06 | Information Disclosure | Linking.openURL target | mitigate | Hardcoded GitHub URL constant; not derived from user input or remote data. |
| T-01-SC | Tampering | npm package installs | mitigate | No new packages; all from Plan 01 audited set. |
</threat_model>

<verification>
- `cd mobile && npx tsc --noEmit` exits 0
- `cd mobile && npx jest` exits 0 (no regressions; regionFilter test from Plan 01 covers the filter logic)
- `grep -rn "router.replace\|router.push" mobile/app/onboarding.tsx mobile/app/zone-picker.tsx` returns nothing (Stack.Protected handles navigation)
</verification>

<success_criteria>
- Trust onboarding shows once with 4 trust points + GitHub link + Comenzar (TRST-01, D-07/08)
- Comenzar and zone selection advance via Stack.Protected (no manual navigation) (D-10)
- Zone picker is state-grouped, searchable, with live status dots (D-11)
- ZonePicker is reusable for Settings "Cambiar zona"
</success_criteria>

<output>
Create `.planning/phases/01-foundation-offline-core/01-03-SUMMARY.md` when done
</output>
