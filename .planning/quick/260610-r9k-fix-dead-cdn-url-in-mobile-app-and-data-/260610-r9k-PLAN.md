---
phase: quick-260610-r9k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/app.json
  - mobile/lib/api.ts
  - .env.example
  - pipeline/main.py
  - tests/test_pipeline_integration.py
  - app/components/mobile/ScreenBajones.tsx
  - app/lib/i18n.ts
  - app/lib/history.ts
  - .github/workflows/collect.yml
autonomous: true
requirements: [QUICK-R9K]
must_haves:
  truths:
    - "Mobile app fetches status.json from the live CDN domain (cocuyo.kralgor.com), not the dead cdn.cocuyo.app"
    - "status.json next_update_approx reflects the real ~2h cron cadence, not a false +10min promise"
    - "Bajones screen no longer presents synthetic random data as real; shows an honest pending state"
    - "History JSON files refresh automatically on a weekly CI schedule and upload to R2 under history/ prefix"
    - "Web history loader can read from a configurable base, defaulting to the existing relative path"
  artifacts:
    - path: mobile/app.json
      provides: "Live statusCdnUrl"
      contains: "cocuyo.kralgor.com"
    - path: pipeline/main.py
      provides: "Honest next_update cadence constant"
    - path: app/components/mobile/ScreenBajones.tsx
      provides: "Honest pending-feature state, no fake HIST data path"
    - path: .github/workflows/collect.yml
      provides: "Weekly backfill_history job uploading to R2"
  key_links:
    - from: mobile/lib/api.ts
      to: status.json CDN
      via: "STATUS_CDN_URL fallback constant"
      pattern: "cocuyo\\.kralgor\\.com"
    - from: app/lib/history.ts
      to: history/{region}.json
      via: "configurable base + relative fallback"
      pattern: "NEXT_PUBLIC_HISTORY_BASE"
---

<objective>
Fix the dead CDN URL in the mobile app and run a data-honesty pass so the apps stop pointing at a non-resolving domain and stop presenting synthetic data as real.

Three independent corrections, one plan:
1. Replace dead `cdn.cocuyo.app` references with the live `https://cocuyo.kralgor.com/status.json`.
2. Make `next_update_approx` honest (cron throttled to ~2h, not 10min) and relabel the Bajones screen as a pending feature instead of showing fake random history.
3. Automate history-file freshness via a weekly CI job that runs `backfill_history.py` and uploads outputs to R2, with a configurable fetch base on the web side.

Purpose: trust integrity. The product's core promise is honesty (anonymous, no fake data). A dead URL silently breaks status fetch; fake Bajones data and a false +10min promise both lie to users.
Output: corrected URLs, honest cadence, honest Bajones UI, CI-driven history backfill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<diagnostic_findings>
Verified by orchestrator 2026-06-10 — do NOT re-derive:
- `cocuyo.app` has NO DNS. `cdn.cocuyo.app` never resolves.
- Live URLs: web `https://app.cocuyo.kralgor.com/`, status `https://cocuyo.kralgor.com/status.json` (200, fresh).
- Dead refs: `mobile/app.json` line 71 `extra.statusCdnUrl`; `mobile/lib/api.ts` line 73 fallback; `.env.example` line 18 `NEXT_PUBLIC_STATUS_URL`.
- GitHub Actions `*/10` cron actually fires ~every 2h (GitHub throttling, observed). `next_update_approx` claims +10min — false.
- History files `app/public/history/*.json` come from `python pipeline/backfill_history.py` (real IODA+NASA+Cloudflare data), frozen at 2026-05-18, manual not CI. Web serves them at relative `/history/{region}.json` via `app/lib/history.ts`.
- `app/components/mobile/ScreenBajones.tsx` renders synthetic random data (`BAJONES_24H` from `app/lib/demoData.ts`) as if real. Decision: label honestly as not-yet-live (bajon detection needs crowd reports; no user base yet).
</diagnostic_findings>

<interfaces>
mobile/lib/api.ts (line 71-73):
  const STATUS_CDN_URL: string =
    (Constants.expoConfig?.extra?.statusCdnUrl as string | undefined) ??
    'https://cdn.cocuyo.app/status.json';

pipeline/main.py:
  _UPDATE_INTERVAL_MIN = 10  # line 46 — Phase 1 cadence
  build_status_json(now, phase, collector_errors, regions):
    next_update = now + timedelta(minutes=_UPDATE_INTERVAL_MIN)   # line 271
    "next_update_approx": next_update.strftime("%Y-%m-%dT%H:%M:%SZ")  # line 276

tests/test_pipeline_integration.py:
  _TOP_KEYS includes "next_update_approx" (line 21). No existing test asserts the
  next_update_approx VALUE — only key presence. Safe to change the constant
  without breaking existing assertions, but ADD a bound-check test.

app/lib/history.ts (line 66):
  const res = await fetch(`/history/${regionKey}.json`);

app/lib/i18n.ts: STRINGS map, entries shaped `{ es: '...', en: '...' }`.
  Existing key `coming_soon: { es: 'Próximamente', en: 'Coming soon' }` (line 151).
  Bajones keys around lines 44-46, 107-115.

app/components/mobile/ScreenBajones.tsx: imports `BAJONES_24H, BajonSeed`
  from '../../lib/demoData', has a `ComingSoonOverlay` component (lines 13-39)
  using `tt('coming_soon', lang)`. The screen renders three blocks (frequency
  trace, event list, summary stats) all driven by synthetic `BAJONES_24H` under
  0.5-opacity overlays.

.github/workflows/collect.yml: the `retrain` job (lines 55-101) shows the
  canonical R2 upload pattern — inline `python - <<'EOF'` with a boto3 client
  built from R2_ENDPOINT_URL / R2_ACCESS_KEY_ID / R2_SECRET_ACCESS_KEY, bucket
  "cocuyo". CF_API_TOKEN is already wired in the `collect` job env (not retrain).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace dead CDN URL with live status.json domain</name>
  <files>mobile/app.json, mobile/lib/api.ts, .env.example</files>
  <action>
Replace every `https://cdn.cocuyo.app/status.json` with `https://cocuyo.kralgor.com/status.json` in three places:
- mobile/app.json: `extra.statusCdnUrl` (line 71).
- mobile/lib/api.ts: the hardcoded fallback string in the `STATUS_CDN_URL` constant (line 73). Keep the `Constants.expoConfig?.extra?.statusCdnUrl ?? '...'` structure; only the fallback literal changes. Update the comment above it ("Falls back to the production CDN URL") to name the live domain.
- .env.example: `NEXT_PUBLIC_STATUS_URL` (line 18).
Do NOT run EAS Update, EAS Build, or any deploy (CLAUDE.md: never deploy during a coding session). The app.json `extra` value is baked at build time, so this change only reaches devices after the next EAS build or EAS Update — record that as a follow-up note in the SUMMARY; do not execute it.
Touch only these three live-config locations; do not alter dead-domain strings anywhere they are intentionally documentary.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leo/claude/cocuyo && ! grep -rn "cdn.cocuyo.app" mobile/app.json mobile/lib/api.ts .env.example && grep -q "cocuyo.kralgor.com/status.json" mobile/app.json && grep -q "cocuyo.kralgor.com/status.json" mobile/lib/api.ts</automated>
  </verify>
  <done>No `cdn.cocuyo.app` remains in the three config files; all three now reference `cocuyo.kralgor.com/status.json`. No build/deploy run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Honest next_update_approx cadence + bound test</name>
  <files>pipeline/main.py, tests/test_pipeline_integration.py</files>
  <behavior>
    - build_status_json sets next_update_approx to now + the honest interval.
    - The honest interval is a conservative bound on the real cadence: GitHub throttles `*/10` cron to ~every 2h observed, so claim 120 min (not 10). This never over-promises freshness; a user is told "next update in ~2h" and any earlier run is a pleasant surprise.
    - Test: next_update_approx parses as ISO UTC and is >= updated_at + 110 min and <= updated_at + 130 min (tolerance band around the 120 min bound).
  </behavior>
  <action>
In pipeline/main.py change the cadence used by build_status_json from 10 min to a conservative 120 min bound reflecting the real throttled cron. Set `_UPDATE_INTERVAL_MIN` from 10 to 120 and update its comment to explain the rationale ("GitHub throttles */10 cron to ~2h; advertise a conservative bound so next_update_approx never over-promises"). Keep using `timedelta(minutes=_UPDATE_INTERVAL_MIN)` at line 271 — only the constant value and comment change. Use the logging module convention already in the file; no print().
In tests/test_pipeline_integration.py add one test in the schema/tracer section that parses `updated_at` and `next_update_approx` (strptime `%Y-%m-%dT%H:%M:%SZ`) and asserts the delta is within 110-130 minutes. Do not weaken or remove existing assertions (`_TOP_KEYS`, phase, scheduler).
Rationale for the conservative-bound option over widening into a range string: the field type is a single ISO timestamp consumed by the typed `StatusJson` interface (mobile/lib/api.ts `next_update_approx: string`); changing it to a range would break the contract. A conservative single timestamp keeps the contract and stays honest.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leo/claude/cocuyo && grep -q "_UPDATE_INTERVAL_MIN = 120" pipeline/main.py && python -m pytest tests/test_pipeline_integration.py -x -q</automated>
  </verify>
  <done>Constant is 120 with explanatory comment; new bound test passes; full integration test file green.</done>
</task>

<task type="auto">
  <name>Task 3: Honest Bajones screen — drop fake data, show pending state</name>
  <files>app/components/mobile/ScreenBajones.tsx, app/lib/i18n.ts</files>
  <action>
Stop presenting synthetic `BAJONES_24H` random data as real grid measurements. Replace the three demo-driven blocks (frequency trace, event list, summary stats) with a single honest pending-feature state.
Add i18n keys to app/lib/i18n.ts STRINGS map (same `{ es, en }` shape; Spanish is primary). Suggested keys:
- `bajones_pending_title`: es "Detección de bajones en camino", en "Voltage-dip detection coming".
- `bajones_pending_body`: es "Esta función necesita reportes de la comunidad para activarse. Aún no hay suficientes usuarios reportando inestabilidad en tu zona.", en "This feature needs community reports to go live. There aren't enough users reporting instability in your area yet."
Reuse the existing serif/mono styling tokens already in ScreenBajones (font-serif title, font-mono caption) and theme colors (`t.ink`, `t.inkFaint`, `t.panel`, `t.line`) so it matches the app. You may keep the existing `SectionLabel` header (`tt('bajones_title', lang)`) above the pending block.
Remove the import and all usage of `BAJONES_24H` / `BajonSeed` / `toTraceEvents` / `FrequencyTrace` / `MiniStat` from this file so no synthetic data path remains. Do NOT delete `app/lib/demoData.ts` (other teaser screens may use it) — only remove this screen's dependence on it. After removal, drop any now-unused imports (`FrequencyTrace`, `MiniStat`, `BajonEvent`); keep `SectionLabel` if you reuse it.
Do not introduce any fabricated "real" numbers. The screen must render zero fabricated measurements.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leo/claude/cocuyo && ! grep -q "BAJONES_24H" app/components/mobile/ScreenBajones.tsx && grep -q "bajones_pending_title" app/lib/i18n.ts && grep -q "bajones_pending_title" app/components/mobile/ScreenBajones.tsx && cd app && npx tsc --noEmit</automated>
  </verify>
  <done>ScreenBajones imports no synthetic data, renders an honest Spanish-primary pending state via new i18n keys, and the app typechecks clean under strict mode.</done>
</task>

<task type="auto">
  <name>Task 4: Weekly history backfill CI job + configurable fetch base</name>
  <files>.github/workflows/collect.yml, app/lib/history.ts, .env.example</files>
  <action>
Automate history freshness and make the web loader read from a configurable base.

CI (.github/workflows/collect.yml):
- Reuse the existing weekly schedule. The `retrain` job already runs Sunday 00:00 UTC (`0 0 * * 0`). Add the history backfill to that same Sunday run rather than adding a new cron line, to avoid more throttled schedules. After the existing model-upload step in the `retrain` job, add two steps: (1) `run: python pipeline/backfill_history.py --days 365` (the script writes to `app/public/history/{region}.json`), (2) an inline `python - <<'EOF'` boto3 upload step modeled exactly on the existing model-upload block — iterate every file matching `app/public/history/*.json` and call `s3.upload_file(path, "cocuyo", f"history/{fname}", ExtraArgs={"ContentType": "application/json"})` so they land under the `history/` key prefix.
- Add `CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}` to the `retrain` job `env:` block (backfill uses it optionally for Cloudflare cross-correlation; the script already treats it as optional).
- Do not add new Python dependencies (boto3 already in requirements). No print() in pipeline source code; the inline CI heredoc may print, matching the existing retrain block.

Web loader (app/lib/history.ts):
- Change the fetch URL from the hardcoded relative `/history/${regionKey}.json` to a configurable base: read `process.env.NEXT_PUBLIC_HISTORY_BASE`; when set, fetch `${base}/${regionKey}.json` (strip a trailing slash on base to avoid `//`); when unset, fall back to the current relative `/history/${regionKey}.json` so the existing static deploy keeps working unchanged. Keep the typed `RegionHistory` return and the existing cache + null-on-error behavior.
- Add `NEXT_PUBLIC_HISTORY_BASE=` (empty default) to .env.example near the other NEXT_PUBLIC vars, with a comment noting the live value would be the R2 history prefix e.g. `https://cocuyo.kralgor.com/history`.
Frontend must not import from pipeline (history.ts only fetches JSON — keep it that way). Pipeline must not import from app/.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leo/claude/cocuyo && grep -q "backfill_history.py" .github/workflows/collect.yml && grep -q 'history/' .github/workflows/collect.yml && grep -q "CF_API_TOKEN" .github/workflows/collect.yml && grep -q "NEXT_PUBLIC_HISTORY_BASE" app/lib/history.ts && grep -q "NEXT_PUBLIC_HISTORY_BASE" .env.example && python -c "import yaml; yaml.safe_load(open('.github/workflows/collect.yml')); print('yaml ok')" && cd app && npx tsc --noEmit</automated>
  </verify>
  <done>retrain job runs backfill and uploads history/*.json to R2 under history/ prefix with CF_API_TOKEN passed; workflow YAML parses; history.ts reads a configurable base with relative fallback; .env.example documents NEXT_PUBLIC_HISTORY_BASE; app typechecks clean.</done>
</task>

</tasks>

<verification>
- `grep -rn "cdn.cocuyo.app" mobile/ .env.example` returns nothing in live-config files.
- `python -m pytest tests/test_pipeline_integration.py -q` green, including new cadence bound test.
- `cd app && npx tsc --noEmit` passes (strict mode) after ScreenBajones and history.ts changes.
- `.github/workflows/collect.yml` parses as valid YAML and contains the backfill run + R2 history upload steps.
- No new Python deps; no print() added to pipeline source; no credentials written to any file; frontend does not import from pipeline.
</verification>

<success_criteria>
- Mobile status fetch points at the live `cocuyo.kralgor.com` domain (no dead URL remaining).
- `next_update_approx` advertises a conservative ~2h bound, never over-promising.
- Bajones screen shows an honest, Spanish-primary pending state with zero fabricated measurements.
- History files refresh weekly via CI and upload to R2 under `history/`; web loader fetches from a configurable base with a working relative fallback.
- All automated verifications pass. No builds or deploys executed (per CLAUDE.md).
</success_criteria>

<output>
Create `.planning/quick/260610-r9k-fix-dead-cdn-url-in-mobile-app-and-data-/260610-r9k-SUMMARY.md` when done.
Include a follow-up note: mobile/app.json change requires an EAS build or EAS Update to reach devices (not run during this session).
</output>
